# 后端业务层收口进度总表（PROGRESS）

> **本文件是"service 补齐 + controller 连线"工作的唯一任务台账**。任何一轮 AI 执行会话开工前必读本文件；
> 台账之外的任务一律不执行。修订本文件须走 §五 变更记录。
>
> 基线：2026-09-21，commit `c482eb0`。分层契约见 `docs/layer-contracts.md`（评审依据，优先级高于本表）。

## 一、执行协议（每轮会话的固定动作）

1. **开工**：读本表 → 取状态为 ⬜ 的最小编号批（或 🔶 批续作）→ 读该批 `specs/0NN-<slug>/` 下的 tasks.md。
2. **批内**：按 tasks.md 任务序推进；service 一律 TDD（红→绿），测试与被测包同目录。
3. **收尾**：跑 §四 DoD 全项 → **更新本表状态（与代码同一 commit）** → commit 格式 `feat(0NN-<slug>): 中文描述`。
4. **遇到范围/依赖问题**：先在 §五 记变更，再动代码；禁止静默扩大范围。

## 二、基线快照（2026-09-21，commit c482eb0）

- **端点总量 218**（admin 129 / shop 42 / user 39 / common 8），API req/res 已全量定义（specs/004-api-surface）。
- **已连线 8 个**（认证纵切片，不进批次）：
  - user：`me` / `logout` / `sms_login` / `wx_login` / `token_refresh`
  - common：`get_captcha` / `verify_captcha` / `get_sms_code`
- **未连线桩 210 个**（`CodeNotImplemented`），分 13 批，见 §三。
- **service 现状**：user/shop/system 三域接口已定义（含业务规则注释）；已实现并有测试：shop 域 `browse / product / cart / order / promotion_calc`（交易链路）与 user 域认证流；其余为"接口已有、impl 待补"，各批启动时在 tasks.md 做精确盘点。
- **生成物现状**：dao/model 全量已生成（69 表，勿手改）；controller 桩全量已生成（`gf gen ctrl`）。
- **验收工具**：`apps/server` 下 `make check-stub` 输出各渠道桩数（无 make 环境可直接执行 Makefile 内等价 shell）。

## 三、批次总表

状态图例：⬜ 未开始 / 🔶 进行中 / ✅ 完成（本批桩清零且 DoD 全过）

| # | 特性目录 | 域 | 端点数 | 依赖 | service 起点 | 状态 | 完成 commit |
|---|---|---|---|---|---|---|---|
| 01 | `specs/007-admin-base` | 后台账户与系统配置（登录/登出/刷新/资料/改密、admin_user、RBAC 角色、config、ping/短信调试桩） | 22 | — | 已实现（含会话 audience 安全修复） | ✅ | `45cd314` |
| 02 | `specs/008-store` | 门店域（自提/核销载体：admin 管理 + common 查询） | 7 | 01 | 已实现（含 sonyflake 首次接线/附近检索） | ✅ | `9146dc6` |
| 03 | `specs/009-logistics-ops` | 物流公司与运营装修（admin logistics/banner/floor + shop banner/floor） | 15 | 01 | 已实现（装修域新建接口/DTO；含投放与装配） | ✅ | `1034ace` |
| 04 | `specs/010-product-admin` | 商品目录后台与 C 端浏览收口（admin spu/sku/类目/品牌/库存 + shop 浏览连线） | 27 | 01 | 已实现（连线收口 + 库存补齐 + 评审修复轮） | ✅ | `d626fd9` |
| 05 | `specs/011-member-center` | 会员中心（资料/地址/收藏/足迹/消息/积分/通知偏好/登录日志/邀请记录） | 21 | 01 | 已实现（8 接口从零实现 + 1 迁移 + 评审修复轮） | ✅ | `a1602a6` |
| 06 | `specs/012-trade-wiring` | 交易闭环连线（shop 购物车/订单/支付 + admin 订单发货/取消/备注 + user 优惠券） | 22 | 02 03 04 | 已实现（连线 14 + 新写 19 方法 + 积分 bug 清偿 + **评审修复轮: 下单端点不可用等 6 处新缺陷已修** + **独立评审 With fixes 逐条修复、lint 归零**） | ✅ | `918e878`/`7923c3a`/`351b169`/`de15462`（详见 §五） |
| 07 | `specs/013-after-sale` | 售后域（shop 申请/撤销售后 + admin 审核/收货/重退款） | 11 | 06 | 已实现（新写 `AfterSaleLogicImpl` 11 方法 + 1 迁移 000038 + 21 测试 + 跨文件回调钩子） | ✅ | `040bf28` |
| 08 | `specs/014-review` | 评价域（shop 发表/追加/我的评价/商品评价列表） | 4 | 06 | 已实现（新写 `ReviewLogicImpl` + 契约微扩 ProductList + 9 测试；**V1 自动通过**见 §五） | ✅ | `119c3fa` |
| 09 | `specs/015-marketing-c` | 营销 C 端（首页聚合 + 秒杀/拼团/砍价/助力/满减列表与玩法动作） | 12 | 06 | 已实现（新建 3 接口/3 实现 + 秒杀欠账清偿 + 端点级可达性测试；**shop 渠道收官**；**独立评审 No→修复轮→复审 Yes**: C1 砍价单全库单行/C2 帮砍并发丢失更新 + I1~I6 与 M 级 20 项全部闭环, 迁移 000041/000042, 详见 §五） | ✅ | `530154e`/`d298028`/`c741ac9` |
| 10 | `specs/016-marketing-admin` | 营销后台（券/秒杀/拼团/砍价/助力/满减管理） | 30 | 04 09 | 已实现（补实现 ICouponLogic 7 方法 + IActivityLogic 28 方法 + 连线 30 桩 + 契约微扩 D3-①~⑨ + 端点级权限探活测试; **独立评审 No→修复轮→复审 Yes**: C1 NULL日期券 panic/C2 已售保护击穿等 6+4 项全闭环, 零迁移, 详见 §五） | ✅ | `26c30c7`/`86efa69`/`adfc67a` |
| 11 | `specs/017-distribution-fund` | 分销与资金（分销账户/申请/关系/佣金规则、提现、邀请记录、分享归因；两级红线已锁表结构） | 23 | 06 | 已实现（IDistributionLogic 16 方法 + admin 12 方法 + 连线 23 桩 + 跨域计提/冲销钩子 + **红线场景 SC-3 全测试化**; **独立评审 No→修复轮→复审 No（4 Important 低成本）→二次收口**, 迁移 000043, 详见 §五） | ✅ | `1ef5688`/`3b346b6`/`3a76790` |
| 12 | `specs/018-member-audit-risk` | 会员管理与审计风控（admin member/登录与操作日志/风控规则与记录） | 13 | 01 11 | 已实现（新建 IMemberAdminLogic + IRiskAdminLogic + 实现 IAuditLogic + 风控评估器装配闭合批次 09/11 降级点 + 连线 13 桩 + 非超管对照 wiring; 零迁移） | ✅ | `6ad4b33`/`ca341e7` |
| 13 | `specs/019-dashboard-final` | 看板与收口终验（dashboard 三看板 + 全量桩清零 + 全量回归） | 3 | 全部 | 已实现（`dashboard_impl.go` 三看板 + 连线 3 桩；**210 桩全部清零**，controller 218 = api 218；**独立评审 No→修复轮**（C1 看板口径含已取消单/C2 迁移 000041 不可逆）全闭环） | ✅ | `3d3c65e`/见 §五 |
| — | （已连线基线） | 认证纵切片 + 验证码/短信 | 8 | — | ✅ 已实现 | ✅ | `ae7acf5` 等 |

合计：210（待做）+ 8（已连线）= 218 ✓（建表时已按批次映射规则对账：13 批覆盖全部 210 桩，无遗漏无重复）

依赖要点：06（交易）依赖 02/03/04 是因为结算试算消费门店（自提）、运费（物流）、商品与库存数据；
09/10 营销拆两批是因为玩法上下文被下单引用（09 先行），后台管理复用同一批 service（10 随后）。

## 四、防偏离条款（执行宪法）

1. **唯一事实源**：本表 + 各批 tasks.md 是唯一任务清单；每轮会话只做当前批清单内的任务，台账外任务不做（发现遗漏 → 走 §五 记录后再做）。
2. **状态与代码同 commit**：批次状态/勾选更新与对应代码进同一个 commit，杜绝表滞后于代码。
3. **批次边界 = 文件边界**：本批只允许改动本批端点对应的 controller 文件、该批 service/测试文件及本表与该批 specs 目录；需要动他批文件 → 停下，先在 §五 登记并调整批次，再继续。
4. **变更先记账**：任何范围变化（增删端点、调整批次/依赖）先写 §五 变更记录，再动代码。
5. **验收数字化**：每批 DoD 以 `make check-stub` 数字为准——本批覆盖端点桩数归零；禁止以"AI 自评完成"替代。
6. **每批 DoD 全项**：
   - [ ] 本批覆盖端点 `CodeNotImplemented` 桩数 = 0（`make check-stub`）
   - [ ] `make test` 全绿（含认证/交易既有测试回归）
   - [ ] `make lint` 通过
   - [ ] 该批 `specs/0NN-<slug>/tasks.md` 任务全勾
   - [ ] 本表状态列更新为 ✅ 并与收尾代码同 commit，commit 前缀 `feat(0NN-<slug>):`

## 五、变更记录

| 日期 | 批次 | 变更内容 | 原因 | commit |
|---|---|---|---|---|
| 2026-09-21 | — | 建表：13 批规划对账（210 桩 + 8 已连线 = 218），新增 `make check-stub` | 初始规划 | `688c08a` |
| 2026-09-21 | 01 | spec 假设修订：允许新增纯数据种子迁移 000035_admin_seed（超管账号 1 个） | 勘察发现无种子管理员，后台无可登录入口；plan/research D3 | `2b0df43` |
| 2026-09-21 | 01 | api 契约微扩：AdminLogoutReq 增加 refreshToken 可选字段 | FR-004 双凭证同毁需要；对齐 user 渠道 LogoutReq 既有形态 | `51b0b6b` |
| 2026-09-21 | 01 | IRBACLogic 微扩 AdminUserDetail 方法 | api 有账号详情端点而接口漏定义；同 D6 模式 | `1ef20f5` |
| 2026-09-21 | 01 | 评审修复轮（With fixes）：C1 访问日志脱敏（accesslog 既有文件，因本批登录/改密端点引入泄露面）；I1 HasPermission 补角色状态过滤（data-model §三 图纸同步勘误） | 代码评审发现，Critical/Important 合并前必修 | |
| 2026-09-21 | 02 | IStoreLogic 微扩 AdminDetail 方法 | api 有门店管理详情端点而接口缺定义；同批次 01 D6 模式 | |
| 2026-09-21 | 02 | model.StoreItem 微扩 ProvinceCode/CityCode 字段 | 两渠道详情 Res 均需省市区全栈而 DTO 仅有 districtCode | |
| 2026-09-21 | 02 | 环境修复：测试库补建缺失的 store 表（另有 merchant/seller/shop 废弃表残留，未删） | 000031 未真正应用（历史迁移改写致账本失真）；非破坏性补建（不做 DROP，废弃表待用户裁定） | |
| 2026-09-21 | 02 | 门店修改定为全量覆盖语义（显式 Fields 白名单强制零值写入） | gf do 的 omitempty 吞零值致无法关闭自提/置歇业；research D6 | |
| 2026-09-21 | 03 | 评审修复轮（No→修）：C1 时间格式化误用 gf 布局（`gtime.Format("2006-01-02T...")` 产出字面串）→ 改标准库 `t.Time.Format`；批次 01 `rbac_impl.rfc3339()` 同源缺陷一并修 | 独立评审实证：admin 时段字段出参为垃圾串，弱断言（只断非空）放过 | |
| 2026-09-21 | 03 | **横切修复**：时区口径统一——DSN 补 `loc=Asia/Shanghai&time_zone='+08:00'`（驱动解释与库会话时钟对齐），修复"读回早 8h、回显再提交每轮漂 8h"；波及全部时间字段读写（config + 5 处测试基座） | 评审实测：Go 进程 +08 而库会话 UTC，gtime 按 Local 解析 UTC 墙钟 | |
| 2026-09-21 | 03 | **C2 已修（用户裁定方案 A）**：应用进程固定 UTC（`main.go` 与 4 处测试基座显式 `time.Local = time.UTC`），与库内 UTC 墙钟及会话时钟对齐；`TestTimeRoundTrip` 由 Skip 转绿（往返同一瞬时、回显再提交不漂移） | 评审 C2 实证：Go Local(+08) 与库 UTC 不一致致读回偏移 8h 且每轮再漂 8h；用户裁定"应用进程统一 UTC" | |
| 2026-09-21 | 04 | 库存实现两处技术决策：①base query 不设 Fields（会被 Count 复用生成 `COUNT(cols...)` 语法错误）；②防负条件不用 `total + (-n) >= 0`（inventory.total 为 INT UNSIGNED, 负数运算触发 out of range 错误码 52）→ 按 delta 符号分支（delta<0 用 `total >= -delta`） | 实现期实证（SQL 报错定位） | |
| 2026-09-21 | 04 | 评审修复轮（With fixes）：C1 分类创建默认启用（DTO 加 Status 引入零值覆盖, 同 doBrand 先例补防护）/ C2 分类更新显式跳过 parent_id·level（api 契约无此二字段, 否则每次改名被搬根+禁用）/ I1 库存扣减防负升级为「可售 >= 扣减量」（原仅护 total, 撞表级 CHECK 报无业务码错误）/ I2 SPU 更新跳过零值关联 ID / I4 C 端排序对齐契约（3=上新, 原实现为价格降序）/ I5 后台品牌列表补软删过滤；补 4 项回归断言 | 独立评审 MySQL 实证：C1/C2 属"放活 005 遗产"时暴露的必然数据破坏；冒烟恰好未覆盖（C 端列表不筛分类 status） | |
| 2026-09-21 | 05 | **跨批次 bug 记账（高优先, 006 必先修）**：`shop/promotion_calc.go:68` 查询 `point_account WHERE user_id=? AND deleted=0`，而该表**无 deleted 列**（仅 V19 建表 + V26 加 last_earned_at）→ 查询必报错 → `calcPointDeductFen` 静默返 0 → **C 端积分抵扣恒为 0** | 011 评审发现（由本批积分语义检查牵出）；属 006 文件边界, 本批不改以免越界 | |
| 2026-09-21 | 06 | 评审修复轮（With fixes）：C1 退款回调列名/状态机双错配（refund_no 应为 after_sale_no；30→40 应为 40→50；affected=0 误记"已处理"）/ C2 回调 success 标志未校验（失败通知可刷成已支付）/ C3 脏态资金无退款对账标记 + 同订单多待支付单未防重 / I1 库存核销未判行数 / I2 留档在事务内且 JSON 列吞错 / I3 pay_order 状态字面量 30 应为 90 + expire_time 无写入 / I6 取消流水列名 operator 不存在致审计全失效 / I7 结算门槛 100 倍维度错误 + 过期券仍可抵扣 / I8 userId=0 哨兵 / I9 购物车 checked 恒写入 / I10 回调应答被中间件包裹 | 独立评审（live DB 列注释 + gf 源码实证） | |
| 2026-09-21 | 06 | 记账补漏（评审建议 6）：I5 券内部能力无出口（ports 未注册 + usableCoupons 恒空）/ I7 门槛口径 / I8 哨兵重载 三条补入本台账 | 此前仅在代码注释提及, 未入台账, 与"变更先记账"协议冲突 | |
| 2026-09-21 | 06 | **勘察发现重大缺口（高优先）**：006 的 `OrderLogicImpl.Create` 实为 **7 步**（非注释所称九步）——步骤 4 计算了券/积分/余额抵扣金额，但**未执行任何实际联动**（无 user_coupon 核销、无 point_log 扣减、无 user_account 冻结）；后果：优惠"算了没扣"，券/积分/余额可被重复使用。**本批范围固定为 22 端点**（含 pay 的库存核销），下单三段联动 + pay 的余额结算涉及账户域（批次 11）→ 一并延后, 已记账待裁决 | 勘察 order_impl.go 全貌确认；属 006 实现缺口的补齐, 非本批 22 端点范围 | |
| 2026-09-21 | 06 | spec 范围修正：FR-006 的"余额消费完成"依赖账户域（`user_account`, 批次 11 实现）→ 本批 pay 回调落地**库存核销**与订单推进；余额结算随批次 11 一并接 | 同上勘察结论 | |
| 2026-09-21 | 05 | 评审修复轮（With fixes）：C1 地址更新静默清默认与未传字段（三态化）/ I1 GetForOrder 返回原始电话（脱敏仅会员边界）/ I3 邀请文案按 reward_trigger / I5 积分回退符号定档 / I4 补内部方法测试与 fixture 清理 | 独立评审实测证据（编辑默认地址即丢默认、下单会拿到掩码电话等） | |
| 2026-09-21 | 05 | spec 假设修订：允许新增迁移 000036（通知偏好表 `user_notify_preference`） | 勘察确认通知偏好**无任何既有存储**（无表无列, 偏好语义需持久化） | |
| 2026-09-21 | 05 | 勘察纠错：曾据 000001 建表语句误判 `user_address` 缺区划码列并新增迁移 000037，实际该三列由 **000023_business_review_fixes** 早已添加（执行时被 Duplicate column 报错拦截）→ 迁移已撤销、账本回落 36 | **教训：列存在性须全局 grep 全部迁移确认，不能只看建表文件**；本次由 DB 约束兜底而非流程拦住 | |
| 2026-09-21 | 05 | 生成物治理：`gf gen dao` 因 CLI 版本差异改写了 6 个既有表的生成物格式（列注释位置, 语义等价）→ **已整体还原, 仅保留新表 4 个生成文件**（最小变更原则） | 避免无关 diff 污染生成物与代码评审面 | |
| 2026-09-21 | 04 | 跨批次债务记账（评审 I3 等）：①**公开路径不解析 Bearer** → `/shop/products/{id}` 的 viewerUserId 恒 0（005 的会员足迹在该链路是死代码, 涉 middleware, 跨批次）；②`AdminSpuListReq.Status` 的 0 同时表"下架"与"全部"（无法只筛下架, 契约问题）；③`AdminSkuCreateWithNo` 二次查询非原子；④库存列表未过滤软删 SKU；⑤`IInventoryLogic` 仍为无实现者死契约（同 I5 记录）；⑥`model.BrandItem` 兼作 C 端/管理端 DTO 职责混 | 评审逐条提出；均不在本批文件边界或需契约变更 | |
| 2026-09-21 | 04 | 连线适配 4 处（按契约最小补充, 全为向后兼容的加字段/加函数, 不改既有签名）：`CategoryInput` +Status；`BrandItem` +Description/Sort/Status；`AdminProductItem` +BrandId；`AdminSkuDetail` +Weight/Barcode；新增 `AdminSkuCreateWithNo`（api 契约需 SkuNo 而既有方法仅返回 id——不改其签名以免破坏 005 测试 5 处调用） | 连线中发现 api 契约字段与既有 DTO/签名不匹配；spec 边界已预告"按契约最小适配并记账" | |
| 2026-09-21 | 04 | 形态说明：库存新写采用**包级函数**（与 009 的 logistics/operation 同形态）；D1 的"跟随 struct"仅指商品域**连线**既有实现 | 保持 shop 域内两类形态的边界清晰（连线跟随既有、新写跟随 009 先例） | |
| 2026-09-21 | 03 | 评审加固：I1 service 层 status 白名单（三处）+ I2 时段清空双语句改事务/补 deleted=0/Count 错误传播 + I4 测试清理改按 id 与 TF2- 前缀归并 | 同批评审实证（status=7/3/9 落库；残留 7 行） | |
| 2026-09-21 | 03 | 记账更正（评审 M1/M6）：`spu_no` 非空列自 000002 起即存在（非 000034；既有 fixture 失效的真实原因是其从未写 spu_no）；补记 api/admin/v1/logistics.go 越界改动 | 评审核查迁移文件与 diff | |
| 2026-09-21 | 03 | spec 契约对齐修正：轮播位置与楼层类型**创建后不可改**（api 的 Update Req 均无这两个字段）；时段清空以 Raw 显式置 NULL | 勘察 api/admin/v1/operation.go；强类型时间列不能承载 Raw（同批次 02 零值教训） | |
| 2026-09-21 | 03 | 接口变更：`ILogisticsLogic` 微扩 Detail；**新建 `IOperationLogic`**（装修域原无接口/DTO） | 装修接口与 DTO 系勘察确认缺失（批次 03 行已标注）；落 service/shop 同域 | |
| 2026-09-21 | 03 | spec 契约对齐修正：物流公司 FR-001~003 去掉"排序"（api 契约无 sort 字段，表 sort 仅内部排序位） | 勘察 api/admin/v1/logistics.go 确认 | |
| 2026-09-21 | 02 | 按用户指示清理测试库三张废弃表（merchant/seller/shop——82f0341 前的多商户概念遗留，各 1 行脚手架种子） | 用户明确授权 DROP；清理后库表 69 = 迁移 68 + schema_migrations，与迁移定义一致 | |
| 2026-09-21 | 02 | **修复横切缺陷（原计划 chore 批，因影响本批验收前移）**：response.mapError 判据由 `gc.Code()>0` 改为契约码域 `>=10001`——gf 框架码（51 校验/52 DB）归一到 10001/10002，恢复日志与追踪号 | 评审 I4 实证：框架码直出致契约码失效（本批 C1/I1 修复本意返 10001 实际返 51）；波及批次 01/02 全部校验与 DB 错误路径 | |
| 2026-09-21 | 02 | 评审修复轮（With fixes）：C1 status 白名单 / I1 全量覆盖护栏 / I2 坐标清零显式置空 / I3 分页越界 / I6 idgen 机器码回退 / M2/M4/M6；另修复横切 I4（错误码映射, 见下条） | 独立评审实证复现；I5（IStoreLogic 无实现者）因涉既有跨批次形态, 记为待裁定 | |
| 2026-09-21 | 06 | 评审修复轮**落地**（前次只记账未改码）：C1/C2/C3/I1/I2/I3/I4/I6/I7/I8/I9/I10 全部改毕；并为每项补回归测试（此前 0 条） | 修复轮代码在工作树中未提交且无测试兜底, 本批续作收口 | |
| 2026-09-21 | 06 | **新发现（Critical, 端点级不可用）**：`Create` 步骤6 订单项快照漏写 4 个非空列（`sku_no/spu_name/sku_name/original_price`, 报 1364）+ 步骤7 状态流水使用**不存在的 `operator` 列**（1054, 与 I6 同根因）→ C 端 `/shop/orders` **每次必失败**。批次 06 对 `Create` **零测试覆盖**, 收口冒烟 6 项恰好绕开下单 → "桩清零"掩盖了端点不可用 | 补测时 RED 实证（`Error 1364: Field 'sku_no' doesn't have a default value`）；教训: `make check-stub` 只证"无桩", 不证"能跑" | |
| 2026-09-21 | 06 | **新发现（Important）**：I11 步骤3 库存锁定只看 error 不判 `RowsAffected` → 条件未命中（库存不足）仍继续下单 = **静默超卖**；秒杀分账分支同病 | 补测时 RED 实证（`EXPECT 52 == 40001`） | |
| 2026-09-21 | 06 | **新发现（I7 补漏, 资金）**：①②券与满减的**门槛**均为"元 vs 分"直接比（100 倍过宽）; ③**抵扣额**未换算, 把"元"当"分"返回（100 倍少抵, 1 元车能白拿满 100 减 20）; ④元→分一律改走 `money.FromYuanString`（`Int64()*100` 会把 99.99 截断为 9900） | 评审仅指出券门槛一处; 按同根因全量复核该文件后补齐（先写测试 RED: `EXPECT 20 == 0` / `20 == 2000`） | |
| 2026-09-21 | 06 | **新发现（I6 补漏, 审计）**：`Cancel` 的 `cancel_type` 恒写 1（后台/超时取消都记成"用户取消"）; `operator_type` 误用 `cancel_type` 枚举（用户取消写成 1=系统）; `userId=0` 把"系统超时"与"管理员"混同; `AdminCancel` 一直丢弃其 `operator` 入参。已按两列各自的契约枚举分离用户/系统/管理员三态, 并让 `operator` 落到 `operator_id` | 补测 RED 实证（`EXPECT 1 == 2`）; 表注释 1系统 2用户 3管理员 vs 修复轮注释误抄 cancel_type 口径 | |
| 2026-09-21 | 06 | **范围变更（用户裁定）**：I5 拆分——本轮只补**券查询口**（`shop.ICouponQuery` + `internal/bootstrap` 装配 user 域实现）, 使 `/shop/cart/checkout` 的 `usableCoupons` 不再恒空; **下单三段联动**（券/积分/余额核销）仍延后至批次 11 账户域。因改动触及 `internal/bootstrap`（跨出本批 controller+service 边界）, 依协议记账 | 券查询口是本批 22 端点自身出参义务, 可与下单联动切开; 核销口属延后范围 | |
| 2026-09-21 | 06 | **记账更正（自我更正）**：C3b 并非"从无到有"——原实现在**提交路径**上确实留下了一条 `process_status=0` 标记（只是写在事务内）; 真实缺陷是留档脆弱（回滚即丢）+ 无告警信号。修复定义为"留档移出事务 + 固定前缀告警", 台账与测试注释均已如实更正 | 以 `git stash` 回退修复前代码实测: 该断言修复前**也通过**, 不能算 red→green | |
| 2026-09-21 | **跨批** | **缺陷记账（高优先, 需迁移裁定）**：`user.level` 列类型 `TINYINT` 与**自身列注释**（"等级ID(user_level_rule.id)"）自相矛盾——被引用的 `user_level_rule.id` 是 `BIGINT UNSIGNED`。后果: 规则表 id > 127 时 `LevelRecalc` 写 `user.level` 报 1264（等级不再更新）, 且 id ≤ 127 时写入的是"规则ID"而非"等级序号"（可能本身即为设计嗅觉问题）。**测试侧**已把自增起点压回以保确定性（`member_fixture_test.go`）; **生产侧修复须由迁移 + 可能需 `gf gen dao` 重生成裁定**（批次 05 记过 gen dao 改写无关文件的坑）, 属批次 05 域, 待用户裁决 | 续作全量验证时实测 1264; 该缺陷使 `make test` 必然失败, 阻塞本批 DoD 验证 | |
| 2026-09-21 | **跨批** | **测试缺陷修复（批次 04 文件）**：`product_impl_test.go` 清理**先删 SKU 再按 SKU 定位库存**（子查询恒空集）→ 库存孤儿行静默泄漏; 孤儿行 `total=0` 恒满足预警条件, 同日累积 98 行后把 `TestInventoryWarnings` 的 100 行分页窗口挤爆, 表现为"等于阈值不命中"的假失败。已修 2 处泄漏点 + 补孤儿自愈 + 一次性清理现存孤儿行 | 全量双跑时实测偶发失败（`EXPECT false == true`）; 根因经 SQL 取证: 孤儿行 98 条、`inventoryBaseQuery` 用 LEFT JOIN、分页窗口 100 | |
| 2026-09-21 | 06 | **横切 lint 基线记账**：`golangci-lint run ./...` 全量非零（22 → 16 条）, 本批文件已清零（清掉 `lineFensOf` 死代码、`do_helpers` reflect.Ptr、`payStatusFailed` 死常量、trade 测试 errcheck）。剩余 16 条全在**跨批文件**（`routes/openapi.go`、`service/user/auth.go`、`product_impl_test.go`、`auth_flow_test.go`）, 待横切 chore 批清理 | PROGRESS §四 DoD 写"make lint 通过", 实际全量从未通过; 各批实按"本批文件零问题"执行, 此处如实记账 | |
| 2026-09-21 | 06 | 测试基建：①新增**本仓首例 controller 层测试**（I8 未登录防御 / I9 三态 / 下单端点 controller→service 全链路）; ②新增 bootstrap 装配测试（I5 端口注入 + 端到端）; ③fixture 清理判据由 `LIKE 'TF-%'` 收紧为精确名——实测 `go test ./...` 并行包下, 该模糊模式会跨包删掉他包的 fixture 数据 | 三处缺陷（I8/I9/下单）只在 controller 层可见或只在装配层可见, service 层测试覆盖不到; 跨包误删经双跑实证 | |
| 2026-09-22 | **跨批** | **裁定项落地（用户裁定"按推荐执行"）**：新增迁移 **000037_user_level_bigint**——`user.level` 由 TINYINT 放宽为 BIGINT UNSIGNED，与其**列注释**（"等级ID(user_level_rule.id)"，被引用列为 BIGINT）对齐；同步移除 `member_fixture_test.go` 里为绕开该缺陷而加的 AUTO_INCREMENT 压回 hack（生产侧已修，测试不再需要绕过）。`gf gen dao` 重生成后 `entity.User.Level` = `uint64`（唯一写入点 `data.Level = levelId` 走 `do.User.Level any`，无破坏） | 原缺陷使规则 id > 127 时 `LevelRecalc` 报 1264 → 会员成长/升级链路失败（测试库必然复现，阻塞 DoD 验证） | |
| 2026-09-22 | **横切** | **数据库统一到应用库 `ecboot`（用户指令）**。起因: 勘察发现**三份事实源**——测试硬编码 `myuser@13306/mydatabase`（容器实例），而应用配置、`make migrate-up`、`gf gen dao` 全指向 `root@3306/ecboot`（本机 MySQL）→ 迁移打 A 库、测试跑 B 库、生成物取自 C 库，同一张表在两侧列集都不同。动作: ① 应用库补齐迁移 35/36/37（原停在 34）；② 7 处测试 DSN 收敛为**单一事实源** `internal/testutil/db.go`（`ECBOOT_TEST_DSN` 可覆盖）；③ `Makefile migrate-fresh` 由容器 13306/verysecret 改到应用库同实例（3306/root）；④ `compose.yaml` 库名 `mydatabase`→`ecboot` 并注明其为可选实例；⑤ `AGENTS.md`+两处 `README` 同步（顺带更正不存在的 `manifest/config/config.yaml` → `config/config.yaml`）。验证: `ecboot` 70 表与 `ecboot_fresh` 全量重放（37 版本、干净）一致；全量测试两连跑全绿 | 用户指令"统一到应用库 ecboot"；不统一则生成物/迁移/测试三者永远互相漂移 | |
| 2026-09-22 | **横切** | **同时修掉一个真实缺陷（时区）**：本机 MySQL 的 `time_zone=SYSTEM` 为 **+08**，而进程按 UTC 写 DATETIME 墙钟 → SQL 端 `NOW()` 比进程时间**快 8 小时**。实测后果: 写"开始时间=+60 分钟"的轮播被 `< NOW()` 判为已开始（`/banners` 在投过滤失效）；推理后果: `CancelTimeout` 的 `created_at < NOW()-30min` 会把**待付款单几乎立即全部取消**。修法（双侧锁 UTC，沿用批次 03 口径）: ① DSN 钉 `?loc=UTC&time_zone=UTC`（同时管住驱动解释与库会话时钟）；② 给本机 MySQL 载入标准时区表（`mysql_tzinfo_to_sql /usr/share/zoneinfo`，此前表在但**0 行** → 命名时区报 1298）。**写法约束已实测写入代码注释**: GoFrame 会把 DSN 参数里的 `+` 变成空格，偏移量写法 `'+00:00'` 必开不了连接——这也**解释了批次 03 当年 `time_zone='+08:00'` 其实从未生效**（会话时钟一直没被真正钉住，当时靠容器 SYSTEM=UTC 侥幸自洽） | 时区缺陷由"测试打到应用库后 `TestPublicBanners` 变红"暴露; 且同一缺陷对应用自身同样成立（非仅测试问题） | |
| 2026-09-22 | **横切** | **独立评审（012 修复轮）结论记账: With fixes**——1 Critical + 5 Important + 4 Minor。Critical: **C3a 的关单逻辑制造了新的静默资损面**（旧支付单被置 90 后，若渠道就旧单扣款成功，回调走"affected=0 → 幂等 SUCCESS"分支：订单不推进、库存不核销、无告警，对账口径也捞不到）。Important: ①`Create` 声明的事务回滚不变量在两条早退路径上不成立（`err` 未赋值 → 连接/事务悬置，经请求 ctx 取消才释放）；②订单项仅补齐非空列，`point_amount/coupon_amount/full_reduction_amount` 三个**行分摊列**仍恒 0（与表契约"合计=订单头对应明细"冲突）；③I1 使秒杀单**永远无法支付**且秒杀价从未接入（`flash_price` 未被读取）、取消不回补 `sold_count`；④台账/图纸不实陈述（I4/I10 声称有回归测试实则零测试；`specs/012-*/data-model.md`/`spec.md` 仍写"30=关闭"）；⑤`catalogTeardown` 是死代码（"批次 04 两处泄漏点"实际只有 1 处活的）。Minor: 测试全局清空 `pay_no=''` 留档、下单测试依赖"库内无在架促销"的环境假设、资金域若干"占位即真相"字段无留痕。**处置: 逐条修复并复验（见后续记录）** | 独立评审 agent 以活库契约 + 临时 worktree 反证实验给出（含 Critical 的可复现输出） | |
| 2026-09-22 | 06 | **评审 Critical 已修**: 回调 `affected=0` 不再一律当幂等——回查该支付单当前状态区分「20 真已处理（幂等应答）」与「90/30 已关闭或失败（渠道就旧单扣款成功，钱真的到了）」。后者**不得应答 SUCCESS**：写 `process_status=0` 留档 + `[资金异常]` 固定前缀告警 + 返回 40006（渠道收到 FAIL 并留痕）。C3b 的对账口径同步收敛为一条可自洽覆盖两形态的判据（"存在 notify_type=1 且 process_status=0 的留档"） | 评审 Critical 实证复现（旧单被置 90 后发成功回调 → 渠道收到 SUCCESS、订单不推进、库存不核销、无告警） | |
| 2026-09-22 | 06 | 回归测试（先红后绿实证）: `TestPayNotifyClosedPayOrder`（修复前 err=nil → 渠道白拿 SUCCESS）；`TestOrderCreateNoTxLeak`（回退提交标志则 `EXPECT false == true`：15 次空购买项早退 = 挂住 15 条连接）；`TestOrderCreateAllocatesPromotionPerLine`（修复前 `EXPECT 0.00 == 1.66`，并钉住"尾差记末行" 166/334） | 三项均由 RED 实证后再修，非事后补测 | |
| 2026-09-22 | 07 | **复审（第二批次）: With fixes —— 1 Critical + 2 Important + 5 Minor, 已逐条修复**。**C2′（我上轮修复引入的新口子, 单线程可复现）**: "40 可重试 + 失败回退 40→30"把刚建立的"**未出款 ⟺ 30**"不变量重新打破——40 的语义是"已发起（钱可能已出）", 回退到 30（可撤）即让"钱可能已出"的记录重新可撤 → 撤销释放额度 → **以新幂等键再退一份**（累计 2× 行实付）。**修法**: 一旦调用过渠道, 失败一律**留在 40** + `fail_reason`（管理员可从 40 重试, 渠道按键幂等去重）; 随之 D1/D2 口径由"失败停 30"改为"**失败停 40**", 30 此后仅表示"已同意待发起"。守卫 `TestAfterSaleRefundFailKeepsRefunding` 已实证能抓（变异回退 30 → `EXPECT 30 == 40`）。**I-1（复审附 SQL 级证据, 推翻我上轮的判断）**: "两处锁互为纵深、单去一处仍绿"**不成立**——承重墙是 `afterSaleUsed` 的**锁定读**: gf 的 DAO 在事务内会先发 `SHOW FULL COLUMNS` 刷新元数据缓存, **而它会提前建立 InnoDB 读视图**（复审实验: A `BEGIN; SHOW FULL COLUMNS` 后 B 插入提交, A 的普通读查不到; 无 SHOW 的对照能查到）→ 普通读不可替代当前读; 订单项行锁提供按行串行与防插入意向锁死锁。注释与台账已按证据改正。**I-2（上轮新引入的锁放大）**: 锁定读在**无 `order_item_id` 索引**的表上全表扫描 + 逐行 X 锁 → 实测"申请 A 行阻塞申请 B 行"（1205）→ 新增**迁移 000039** 补 `idx_order_item`; 修复后 B 行 447ms 成功（此前 2s 超时）。**Minor**: M-1 `inventory_log` 回补流水无 fixture 清理（既有 57 行残留, 同批次 04 孤儿库存行模式）→ 补清理; M-2 清理健壮化; M-3 重复退款回调留档标志改 false（对齐列注释"0未处理或重复忽略"）; M-4 记账: 库存行缺失会使完成**永久卡 40**（有留档+告警但无人工收口路径）→ 数据修复/人工收口待办; M-5 C1 并发守卫补错误码断言（失败必须是 40008, 杜绝 1205/10002 混入掩盖锁问题） | 复审以活库锁实验 + 变异 + SQL 级证据给出; C2′ 有单线程可复现路径 | |
| 2026-09-22 | 07 | **测试盲区已消除（复审建议落地）**: 用"**外部事务持锁**"的零生产测试缝构造, 确定性地钉住 `Cancel` 与 `tryRefund` 的条件更新语义: 外部事务先改状态不提交（持行 X 锁）→ 目标方法的前置**普通读**仍见旧状态（MVCC）→ 其条件 UPDATE 阻塞 → 外部事务提交 → 条件重新求值不命中 → 必须 40006 且**不得覆盖状态**; 对 `tryRefund` 还须**渠道调用 0 次**（"未赢得状态迁移就不得出款"）。两条均经**变异实验实证能抓**（去掉各自的条件 WHERE → `EXPECT -1 == 40006`）。**关键经验**: 构造必须**先做连接池预热**——否则目标 goroutine 可能因取不到连接而"迟到", 其前置读落在外部提交之后, 构造即失去区分力（我首两次尝试因此假绿, 已定位并修正） | 复审给出该构造并实证; 本人复现 + 定位假绿原因 | |
| 2026-09-22 | 06 | **评审 Important 修复**: ①`Create` 回滚改**提交标志**（不再依赖"每条错误路径都给 err 赋值"的不变量，消除连接/事务悬置）；②订单项 `coupon_amount`/`full_reduction_amount`/`point_amount` 按**各自构成**分摊（保和 + 尾差记末行），`promotion_amount` 取三者之和；③秒杀路径**明确拒绝**（`FlashSaleItemId>0` → 50002）并删除原分支——它产出的单永远无法支付、按原价计费、且从不读取 `flash_price`；**批次 09 待办清单已写入代码注释**：接通秒杀价快照（`flash_price`→`trade_order_item.price`）+ 同时锁 `inventory` + `Create` 校验活动项 `sku_id` 与下单行一致 + 取消时回补 `sold_count`（`cancelBy` 现只回补 `inventory.locked`）；④图纸勘误: `data-model.md`/`spec.md` 的 pay_order 状态由"30 关闭"改为"30 支付失败 / 90 已关闭"（图纸正是下一轮抄错的源头） | 评审以活库契约逐条实证（含 `flash_sale_item`/000018 契约、Postel 收敛） | |
| 2026-09-22 | 06 | **评审 Minor 修复 + lint 基线归零**: ①测试清理匿名留档由"全库 `WHERE pay_no=''`"改为按 **id 窗口**（避免误伤并行包）；②下单/控制器金额断言改为**恒等式**（`pay = total - promotion`）而非写死 "20.00"，去掉"库内无在架促销"的环境假设；③清理死代码——`catalogSuite`/`catalogTeardown`（批次 04 遗留，长期无人调用）、`routes/openapi.go`（44 行无人调用的 `detectPublic`）、`user/auth.go` 4 个未使用常量、测试 errcheck。**结果: 全量 `golangci-lint` 22 → 0 issues**——§四 DoD 的"make lint 通过"首次真正成立 | 评审 Minor 逐条 + 我此前建议的横切 chore | |
| 2026-09-22 | 06 | 新增**本仓首个 HTTP 端点级测试** `internal/routes/notify_ack_test.go`（真实路由 + 完整中间件链）: 断言回调应答是渠道自己的裸 JSON。RED 实证修复前渠道实收 `{"code":0,"data":{"code":"FAIL","message":"回调报文非法"},"message":"ok"}`——**包装成成功语义且泄露内部错误文案**，正是 I10 所述"失败→重试"语义失效 | I4/I10 此前被声称"已补回归测试"实则零测试（评审 Important ⑤），本轮补齐 | |
| 2026-09-22 | **07** | **正式开工批次 07（售后域）+ 范围变更记账**：本批按 §一.1 取最小编号 ⬜ 批执行；勘察发现 `specs/013-after-sale/` **不存在**，故先按 Spec Kit 流程补齐契约（specify→plan，制品见该目录）。**范围变更（先记账后动码）**：spec 初稿假设"零迁移"，Phase 0 实证推翻——`IAfterSaleLogic` 的 `Approve/Reject/ConfirmReceipt/RetryRefund` **四个方法都接收 `operator` 而 `after_sale_order` 无任何列可承载**（`admin_operation_log` 是请求级审计表但至今无人写入，其列表端点属批次 12），不补即重蹈"审计被静默丢弃"（批次 06 评审同型 Important）；故新增**唯一迁移 000038**（`after_sale_order` + `operator_id` + `fail_reason` 两列）。同时记账两处口径裁定：①**撤销边界由用户裁定收窄**为"待审核/待寄回/待退款 可撤"，排除"退款中"（在途退款不可中断，原注释"未终态→91撤销"据此修正）；②退款发起时机=审核通过/确认收货的同一动作内立即发起，失败停 30 留 `fail_reason`、由后台重试 | 用户指令"回归 PROGRESS 主线继续执行"；迁移变更依协议 §一.4 先记账（先例：批次 01 加 000035、批次 05 加 000036） | |
| 2026-09-22 | 07 | **批次 07 实施完成（11 端点全清）**: 新写 `AfterSaleLogicImpl`（Apply/List/Detail/Cancel/SubmitReturn + AdminList/AdminDetail/Approve/Reject/ConfirmReceipt/RetryRefund 共 11 方法）、连线 shop 5 + admin 6 桩；`check-stub` 对账 **shop 21→16、admin 64→58**（四渠道合计 96→85）。TDD 新增 **21 个测试函数**（申请校验矩阵/额度释放/尾差消解/审核两分支/状态机守卫/寄回确认/回调幂等/渠道失败留痕重试/0 元边界/撤销边界/列表详情归属/完成副作用）。验证: `go test ./...` 两连跑全绿（既有链路零退化）、`golangci-lint run` **0 issues**、迁移全量重放（`ecboot_fresh`）至版本 38 | 总表 §一.1 主线段推进; DoD 数字达标 | |
| 2026-09-22 | 07 | **实现期偏离三处（依协议先记账）**: ①**跨文件钩子**——`pay_impl.go` 的 `HandleRefundNotify` 由单条 UPDATE 改为**事务包裹**并在条件更新命中时调用 `afterSaleFinishedSideEffects`；不改则渠道回调推进到 50 时**永不回补库存、不更新订单 refund_status**（SC-005 账实一致无法成立），tasks 原写"不改它"系勘漏。②`ports.go` 新增 `ICommissionReverse` 事件出口（售后完成投递佣金冲销，结算属批次 11，本批只投递）。③DTO 微扩：`AfterSaleSummary`/`AfterSaleDetail` 各 +`UserId`（后台列表/详情契约要求；C 端不映射。同批次 04"按契约最小适配"先例） | 三处均为"契约要求的字段/钩子缺失"，非扩大范围；实现前已核对 | |
| 2026-09-22 | 07 | **未凭猜的核对结果（记账为证）**: 实现前核对 `aftersale:read/audit/refund` 权限点——**已在 000032 RBAC 种子与 `consts/permission.go` 中就位**，故无需权限种子迁移（避免了"再补一个迁移"的过度动作）；`is_super` 直通已验证。另: `paychannel.Channel.Refund(outRefundNo, amountFen)` 既有且注释即"幂等: outRefundNo"，与"售后单号作幂等键"天然吻合，无需新渠道抽象 | 验证式实现（先核对再动手）——本条为方法论留痕 | |
| 2026-09-22 | 07 | **独立评审（第二批）: With fixes → 已逐条修复**。**Critical 两条（评审均实证）**: ①**C1 申请无并发防线**——`Apply` 的"读累计额度→插入"无事务无锁, 并发/重复提交可落多张超额售后单（评审实测: 行实付 10.00 的订单项落 3 张各 10.00 的单、逐张审核 = **3 倍出款**）→ 改为**行锁事务**（`trade_order_item` 行 `FOR UPDATE` + 占用集合锁定读, 两处独立串行点）; ②**C2 在途退款可被撤销**——原实现**先调渠道再改状态**, 于是"钱已出"的判据是"渠道是否返回"而非状态 40, 买家可在窗口内撤销（终态 91 + 额度释放 + 可再退一次）→ 改为**先条件推进 30→40、再调渠道**, 渠道失败则条件回退 40→30 + `fail_reason`; 并允许从 40 重试（渠道以 outRefundNo 幂等，用于自愈"40 但钱未出"的崩溃窗）。**Important 六条**: I1 回补绕过 `inventory_log`（000003 为"6=售后回补"留了枚举）且不判行数 → 补流水 + 判行数; I2 完成副作用失败时不留档不告警（钱已出而记录卡 40）→ 事务失败分支补留档 + `[资金异常]`; I3 新增的 `operator_id/fail_reason` 无读取路径（审计"只做了一半"）→ DTO+api+控制器打通（含 `audit_time/refund_time`）; I4 `spec.md` 仍写"不新增迁移"而 research/plan 声称已修正 → 实为**未修正**, 已改; I5 原 21 个测试对"条件更新"铁律零覆盖（评审三处变异后仍全绿）→ 补 5 个并发/在途守卫测试; I6 售后用例回调留档（`pay_no=PAY-AS*`）永不回收（实测残留 **92 行**）→ 按前缀清理 + 一次性清理。**Minor 七条**: M1 渠道失败静默返 nil 且吞错 → 返**新增错误码 40013**（`CodeRefundFailed`）+ 留痕; M2 重复回调返 40009 致渠道无限重试 → 区分为**幂等应答成功**（状态 50）; M3 图纸金额公式与实现口径不符 → 按实现订正; M4 契约图纸权限名写 `aftersale:list` 而实现/种子是 `aftersale:read` → 订正; M5 `tasks.md` T026 漏勾 → 补; M7 `ConfirmReceipt` 注释仍写"→30" → 订正; M6 **跨批观察（记账待办）**: 退款回调端点无验签、无金额校验（对比支付回调），而本批让它触发**不可逆副作用**（回补/冲销/退款状态）→ **接真实渠道前必修** | 独立评审（活库契约 + 临时 worktree 探针 + 变异实验）; 两条 Critical 均有可复现证据 | |
| 2026-09-22 | 07 | **测试能力边界如实记账（评审 I5 的余项）**: 补的 5 个守卫中, **C1 与 C2 的守卫已实证能把变异打红**（C1: 去掉两处锁 → 4 并发落 4 张; C2: 变异回"先调渠道" → `EXPECT 30 == 40`; 另 `refund_status` 只统计已完成的守卫亦已实证）。但 **`Cancel` 与 `tryRefund` 的"条件更新 WHERE"变异黑盒测不出**——两者临界区仅两条语句（约 1ms），并发调度难以稳定命中窗口; 该不变量目前由**服务层前置状态校验 + 代码写法**保证，测试只覆盖"恰好一次生效"的粗粒度语义。此为已知测试盲区, 记录在案 | 自行变异实验（与评审方法一致）; 宁可如实记录能力边界, 不写"看起来很绿"的假保证 | 　→ **该盲区已于下一轮消除**（见本表"测试盲区已消除"行） |
| 2026-09-22 | **08** | **批次 08 开工 + 两处记账**（依协议先记账再动码）: ①**契约微扩**——第 4 个端点（商品评价列表）在 `IReviewLogic` 里**无对应方法**（接口只有 Create/Extra/MyList）→ 按批次 01/02/04"按契约最小适配"先例补 `ProductList` 并记账; ②**合规债务（高优先, 公开上线前必修）**——本批只有 C 端 4 端点、`api/` 无后台审核契约（13 批规划内无后台评价管理批），而既有契约是"默认待审 + 仅审核通过可见" → 严格实现会使**商品评价列表恒为空**（无人能审核）。**用户裁定 B（2026-09-22）：V1 自动通过**（新评价 `audit_status=1`），评价即刻可用; 审核字段与 `IReviewLogic.Audit` 能力保留，**公开上线前必须补后台审核面（审核列表/通过/驳回/商家回复）并切回"先审后发"**——UGC 未经审核即公开属中国平台内容审核责任缺口，此债务记账在案 | 用户指令"启动批次 08"; 契约缺口与合规债务均按 §一.4 先记账 | |
| 2026-09-22 | 08 | **批次 08 实施完成（4 端点全清）**: 新写 `ReviewLogicImpl`（Create/Extra/MyList + 微扩 ProductList）、连线 shop 4 桩；`check-stub` 对账 **shop 16→12**（四渠道合计 85→81）。TDD 新增 **9 个测试函数**（提交矩阵/并发一项一评/列表与汇总同源同筛/星级筛选/匿名与昵称脱敏/我的评价含全状态/追评 90 天边界与并发）。验证: `go test ./...` 两连跑全绿（既有链路零退化）、`golangci-lint` **0 issues**、**零迁移零新 DTO 零生成物变更** | 总表 §一.1 主线段推进; DoD 数字达标 | |
| 2026-09-22 | 08 | **新发现（记账待裁定）**: api 的 `ReviewExtraReq.Images`（追评图片）**既无存储列**（`product_review` 无 `extra_images`）**也无出参字段**（`ReviewItem`/`MyReviewItem` 均无 extraImages）——契约自相矛盾（疑似从提交评价的请求拷贝而来）。本批**不为其新增列**（那会写入永远无法展示的数据，且属 API 契约变更），该参数当前被忽略并在实现处注释说明；若要支持追评图片，需同时补「表列 + 两个出参字段」，待用户裁定 | 勘察 api 契约与活库列时发现；宁可显式记录也不静默丢字段 | |
| 2026-09-22 | 08 | **独立评审（第三批次）: With fixes —— 1 Critical + 3 Important + 13 Minor, 已逐条修复**。**C1（Critical, 直接推翻本批"4 端点全清"的交付结论）**: 4 个端点里**2 个在真实 HTTP 链路上恒返回 10003**——`middleware/auth.go` 的 `publicPrefixes` 含前缀 `"/shop/reviews"`（段边界匹配）, 把 `/shop/reviews/mine`（我的评价）与 `/shop/reviews/{id}/extra`（追评）一并当公开路径**直通**（`Auth` 对白名单路径不校验 Bearer、**不注入 ctx userId**）→ 控制器 `requireMember` 恒判未登录; 另有一处 `POST /shop/reviews` 的精确例外（历史遗留）。**修法**: 删除该白名单行（公开的商品评价列表由 `/shop/products` 前缀覆盖）与已成死代码的例外。**并新增端点级可达性测试** `internal/routes/review_wiring_test.go`（真实路由 + 完整中间件链 + 真实 controller + **合法会员凭证**逐个探活, 任何端点回 10003/10002 即失败）, **反向验证该测试能抓住本缺陷**（恢复白名单行 → 立即红）。**此改动跨批文件（middleware/auth.go）已按协议记账**。**教训（与批次 06 的 C4a/C4b 同源）**: "桩清零"只证明没有 `CodeNotImplemented`, **不证明端点可达**; service 层测试直调 service（自带 userId 参数）, 看不到"中间件是否把身份注入 ctx"这一跳——端点级验证必须走到 HTTP。该白名单行为历史遗留, 但**是本批把它从不可观测变成用户可见故障**（此前 4 端点都返回未实现码） | 独立评审以真实路由探针 + 有效凭证实证（含成功/失败对照） | |
| 2026-09-22 | 08 | **Important 三条已修**: I1 测试泄漏——`review_impl_test.go` 的"89 天边界"用例 seed 用了另一个单号而清理按 `f.OrderNo` 删 → **每次跑测试在活库留 1 行**（评审实测累积 6 行, 且这些行 `audit_status=1/deleted=0`, 对任何查该 SPU 的路径都是"看起来像真实通过评价"的脏数据）→ 改用 `f.OrderNo` + 清理既有残留; 文件头"全部自清"改为与事实相符。I2 **同一 SPU 的均分在两处口径不同**（评审实测 `sum=13/4条` 时 评价列表 `3.2` vs 商品详情 `3.3`）——本批 Go 侧 `FormatFloat`(半进位规则) 与详情页 SQL `ROUND(AVG(...))` 不一致 → **统一到 SQL 口径**并让空集合占位与详情页同为 `"—"`（补断言）。I3 超长 UGC（>列宽 1024）→ MySQL 1406 → 被归一为 **10002 系统繁忙**（用户无从得知"内容太长", 重试永不成功）→ api 两处 `Content` 补 `v:"max-length:1024"`; 并记**跨批缺口**: `api/` 全仓此前 `max-length` 用量为 0（共性缺口, 建议单独记账） | 评审以变异实验 + 活库数据实测 | |
| 2026-09-22 | 08 | **Minor 十三条**: 其中**六处"变异存活"已补断言并逐条变异验证能抓**——`deleted=0` 过滤（列表/我的/追评）、**追评 90 天边界 `>=`**（新增"恰好 90 天可追评"）、我的评价排序（`OrderDesc`）、空昵称占位、图片写入与解析往返、追评 affected=0 的两条文案可区分; 另修: **匿名评价不再参与昵称查询**（最小化原则）+ 软删用户昵称不展示; 文档笔误更正（`data-model` 的 40009→10006、`quickstart` 的 40010→40006）。**记账待办（未做, 各有理由）**: ①**软删评价与唯一索引的语义分裂**（`deleted=1` 后前置校验说"未评过"但 `uk_order_item` 会拦下 → 用户删除评价路径落地后会变成"删了永远不能再评"）; ②追评**不看审核状态**（待审/驳回也可追评, V1 无实害, 后台上线后需明确）; ③**FR-010 对"我的评价"不可满足**（要求含图片/规格, 而 `MyReviewItem` 无这两个字段——api 契约缺口, 本轮对 service 接口的微扩记得很清楚而这条漏记）; ④`IReviewLogic` **全仓无实现者**（只有声明 + 注释引用, 故 `ReviewLogicImpl` 缺三个后台方法不会有编译错误, "实现接口"是文字承诺——与 `ICartLogic` 等同形的仓内风格）; ⑤`isDupKey` 与同包两处内联 1062 判断重复（`product_impl.go`/`favorite_impl.go`）可收敛 | 评审以 20 项变异实验逐条给出; 能修的都修了, 需契约/后续批次决定的如实记账 | |
| 2026-09-22 | **09** | **批次 09 开工 + 四处记账（依协议先记账再动码）**: 本批 12 端点、**收官 shop 渠道**（12→0）。勘察发现并记账：①**C 端营销契约整体缺失**——`IActivityLogic` 全是管理面方法, C 端列表/玩法动作**无任何接口与 DTO** → 新建 `IMarketingLogic`/`IBargainLogic`/`IAssistLogic` 三接口与约 10 个 C 端 DTO（同批次 03 因装修域缺契约而新建 `IOperationLogic` 的先例）; ②**`/index` 是脚手架占位契约**（`IndexRes{Msg string}`）→ 经用户裁定扩为**聚合结构**（轮播+楼层+五类活动入口+可领券, 只用现有能力组合, 不新建表/DTO）, 属 **api 契约变更**; ③**助力发奖跨域**（`reward_type` 1券/2积分, 依赖 user 域）→ 经用户裁定本批只**按端口投递发奖意图**（`IAssistReward`, 未装配降级告警, 同批次 07 的 `ICommissionReverse` 形态）; ④**风控挂载**（迁移 000029 两处明写"砍价/助力是被刷重灾区——入口须挂风控"）→ 新建端口 `IRiskHit`（实现属批次 12, 未装配放行+告警）。另记: **§一.4 跨批改动**——`order_impl.go` 的秒杀分支与 `cancelBy`（批次 07 ledger 明确移交本批清偿, 删除条件=秒杀价快照+inventory锁+取消回补 sold_count 全部落地） | 用户指令"启动批次 09"; 四处均按协议先记账 | |
| 2026-09-22 | 09 | **第五处记账（实现期发现, 先记账再动码）: 新增迁移 000040** —— `trade_order` 补 `flash_sale_item_id BIGINT UNSIGNED NULL`。原因: 秒杀订单取消要**回补活动库存**（批次 07 欠账的清偿条件之一）, 而订单表**没有记录该单属于哪个场次商品**; 若改用"按 sku 反查进行中的场次"会在"活动已结束才取消"时漏回补 → 活动库存永久泄漏。故按批次 01/05/07 先例补列（spec 原假设"零迁移"据此修订） | 实现期勘察确认字段缺失; 依 §一.4 先记账 | |
| 2026-09-22 | 09 | **批次 09 实施完成（12 端点清零, shop 渠道收官）**: 新建 `IMarketingLogic`（5 公开列表 + 首页聚合）/`IBargainLogic`（发起/进度/帮砍）/`IAssistLogic`（发起/进度/助力）+ `marketing_impl.go`/`marketing_lists_impl.go`/`bargain_impl.go`/`assist_impl.go`; 连线 12 桩; 新增 2 端口（`IRiskHit` 风控 / `IAssistReward` 发奖意图）+ ~11 个 C 端 DTO; **跨批清偿秒杀欠账**（秒杀价快照/活动+商品双库存/取消双回补/删除批次 07 临时拦截）; 新增迁移 000040（订单记录场次商品 id, 取消时据此回补）。`check-stub` 对账: **shop 12→0（渠道收官）**, 四渠道合计 81→69。测试: 新增 4 个测试文件/15 个函数（秒杀链路含**支付回调核销成功**——批次 07 欠账的直接验收、砍价尾差与并发、助力达标与并发、列表与首页、端点级可达性）。验证: `go test ./...` 两连跑全绿（01~08 零退化）、`golangci-lint` **0 issues** | 用户指令"启动批次 09"; DoD 数字达标 | |
| 2026-09-22 | 09 | **实现期新发现（两处由测试当场抓到, 非评审带出）**: ①**白名单吞掉会员动作**——`/shop/bargains/`、`/shop/assists/` 前缀级放行会把**会员动作**（`/{id}/cut`、`/{id}/helpers`）也当公开路径直通（Auth 不注入 userId → 控制器恒判未登录）, 与批次 08 的 C1 **同型**; 本次由**全端点扫描**（41 个 shop 端点 × 白名单前缀, 找出 4 处非 GET 命中: 2 处故意的支付/退款回调 + 2 处真缺陷）发现 → 新增 `publicGetOnlyPrefixes` 规则（**同前缀下的公开读只放行 GET**）+ 端点级可达性测试 `marketing_wiring_test.go`（公开端点游客可达 / 会员端点凭证可达 / **不带凭证必须 10003** 三向断言）, 并**实证能抓回归**（把两个前缀加回白名单 → 立即红）。同时测试还抓到 `/shop/index` **漏配白名单**（游客访问首页回 10003）→ 已补。②**助力达标判定用了自增前的旧计数**——并发助力时每个请求都看到旧计数, 谁都判不出"达标", 记录永远停在"进行中"（并发用例 `EXPECT 1 == 2` 抓到）→ 改为自增后**重读最新计数**再判, 条件置位者才投递发奖意图（并发只一次） | 测试驱动开发当场暴露; 两处若不修都是"功能上线即不可用/不生效"级 | |
| 2026-09-22 | 09 | **契约与口径的四处实现期调整（记账）**: ①`api/shop/v1/index.go` 的 `IndexRes` 由脚手架占位（`Msg`）按用户裁定扩为**聚合结构**（复用各列表端点的既有类型, 不新造分块类型）; ②`BargainSku` 补 `itemId`（发起砍价必须传场次商品 id, 而契约原本只给 skuId）+ `maxCutCount`; ③`ActivitySkuBrief` 补 `ItemId`; ④**首页"可领券"口径**——`ICouponLogic` **全仓无实现**（其方法与 admin 券端点属批次 10）, 且 C 端无 shop 券端点 → 改为 shop 域**直读券表**（**既有先例**: `promotion_calc.go` 早已直读 coupon/user_coupon 计价）, 口径为"模板级: 启用未删且未领完", 与 user 域 `/user/coupons/available` 的"会员级: 叠加个人限领"**回答不同问题故不强行统一**（已在代码注释说明, 若后续要把会员级口径搬上首页应走既有 `ICouponQuery` 端口）。另: `bargain_activity` **无发起次数字段**（`assist_activity.per_limit` 才有）→ 砍价侧不设发起次数限制, spec FR-007 相应降级 | 实现期勘察; 均属"按契约最小适配"并记账 | |
| 2026-09-22 | 09 | **独立复审（修复轮）: Yes, 22 项全部闭环**——C1/C2/I1~I6 每项均有复审员独立探针证据（真实库/HTTP 端点级；C2、I4 变异实验证明交付测试具备 RED 能力），M2~M13 处置属实、M8 记账合规；全量两连跑 0 失败、lint 0 issues、桩数一致、生成物干净、无残留。**复审新发现 8 项（无 Critical, 不阻塞）**, 处置: **就地修 2 项**——①新发现-2 砍价 Launch 落库与首刀流水不同事务（流水插入失败会残留 cut_count=1 无流水, 恰违反本轮新立的守恒不变量, M2 只收编了 Cut/Help 漏了 Launch）→ 收进同一事务; ②新发现-7 步骤5.5 的并发防双花无交付测试钉住（串行重复下单由事务外前置校验兜住, 并发属性只有复审探针验证过）→ 补并发用例。**记账待批次 10/13 六项**: ③【Important, 优先级高于 N+1】满减 scope 只进展示、**下单计价不认范围**——`calcFullReductionFen`（012 文件）完全不看 promotion_activity_scope, "指定商品满 100 减 10"对任意商品生效, 且 `ORDER BY a.id LIMIT 1` 只取 id 最小活动非最优档; I3 修复后 C 端明示"指定商品/分类"而结账不认, 契约从"无信息"变"错误信息"; ④FlashSaleItemId 与 BargainRecordId 可同传（dto 注释"互斥"无校验, 砍价成交单额外吃掉秒杀名额, 无资损属防篡改面）; ⑤per_limit 计数为快照读无串行化（同会员并发双 token 可超限, 自伤面）; ⑥砍价成交单取消后权益作废不回退（bargain_record 恒 3, FR-006 未定义属策略裁定）; ⑦Cut 事务内不复核超时（极小窗口, 下单侧有超时拦截无资损）; ⑧fullReductionScopeHits 重复执行同条件活动查询（与 M8 同族） | 复审以独立探针 + 变异实验 + 全量验证给出; 就地修的两项均为本轮修复自身的自洽缺口 | |
| 2026-09-22 | **10** | **批次 10 开工（用户"回归 PROGRESS 主线"持续指令, 沿批次 07 先例自动取最小编号 ⬜ 批）+ 开工勘察记账**: 本批 30 端点全在 admin 渠道——券 6（`/admin/coupons` CRUD + `/coupons/{id}/records` + detail）/ 满减 5（CRUD+detail）/ 拼团·秒杀·砍价各 5（CRUD+`/{id}/items` 场次商品设置）/ 助力 4（CRUD）; 控制器桩 30 个（admin 58 中的 30）。**接口现状**: `ICouponLogic`（6 管理方法+PublicList）与 `IActivityLogic`（满减 5+拼团 6+秒杀 6+砍价 6+助力 5=28 方法）**接口已有、全仓无实现者**; `specs/016-marketing-admin/` **不存在** → 先按 Spec Kit 流程补齐契约（specify→plan→tasks, 批次 07 同例）。**已知约束入 spec**: ①批次 09 复审移交——满减 scope 下单计价不认范围（Important, 下单侧属 012 文件, 本批先把"配置面"做对: scope 行创建/更新/详情全链路）; ②assist per_limit 语义被 uk_activity_user 钉死"每人一次", 后台配置 >1 不生效须在契约注释声明; ③满减档位 uk_activity_threshold 唯一（50008 预留码）; ④列表 N+1（M8/复审-8）本批一并收（同文件域内）。权限点核对在建契约时做（批次 07 先例: 先核对 000032 种子再决定是否需要权限迁移） | 批次 09 修复轮+复审已闭环（Yes）; §一.1 取最小编号批 | |
| 2026-09-22 | 10 | **批次 10 实施完成（30 端点全清）**: 新写 `promotion_impl.go`（券 7 方法, 含契约微扩 AdminDetail）+ `activity_impl.go`（满减 5/拼团 6/秒杀 6/砍价 6/助力 5=28 方法）; 连线 30 桩。**契约微扩 D3-①~⑧（全部记账）**: 接口 +AdminDetail; `CouponTemplate`+ValidType; `CouponInput`/`PromotionActivityInput`/`GroupBuyInput`/`BargainActivityInput`/`AssistActivityInput` 各+Status（api Update Req 有而 DTO 漏, 零值启停语义用"必传 status"契约约定）; `PromotionActivityItem`+GroupSize/PerLimit/RewardType/RequiredCount/RewardDesc（各玩法列表项出参）。**实现期新发现（测试当场抓到）**: ①拼团表时间列为 `valid_start_at/valid_end_at`（与其余活动表不同名, 000017）→ 通用函数时间列参数化; ②同值 UPDATE affected=0 误判"不存在"（MySQL 不计同值行）→ 存在性前置 Count + 更新幂等成功（批次 02 先例）。**防线**: 满减档位 50008 双路（前置查重+1062 兜底）/场次商品 1062→50002/**秒杀已售保护**（事务内 LockUpdate 复核 sold_count>0 行不可移除）/全量替换事务先删后插/金额 FromYuanString。**端点级测试**: `marketing_admin_wiring_test.go` 30 端点全量探活（超管直通不回 10003/10005 + 未登录对照必须 10003）——RequirePerm 挂接漏洞的守卫。**C 端联动（SC-3）**: 停发/软删 → C 端首页券/满减/秒杀/助力列表即时消失（断言钉住）。验证: `go test ./...` 两连跑全绿、`golangci-lint` **0 issues**、`check-stub` **admin 58→28**（四渠道 69→39）、零迁移零权限种子（000032 就位） | DoD 数字达标; 批次 09 复审移交的 6 项记账中④~⑧随本批域继续挂账（满减计价认范围③属 012 文件仍挂账） | |**C1 砍价单全库单行**: `bargain_record.uk_order_no(order_no)` + `NOT NULL DEFAULT ''` 使第二人发起必撞 1062→10002（评审双会员探针实证; 交付测试每用例恰好只建一张而全绿）→ **迁移 000041**: 既有 `''` 规整为 NULL + 列改可空（"下单后回填"本就是 NULL 语义, NULL 不参与唯一约束）; `bargain_no` 改 sonyflake（原"秒级时间戳+uid%10000"同用户同秒必撞 uk_bargain_no）; 1062→业务码。**C2 帮砍并发丢失更新**: 条件更新守卫只有 status=1, 中间刀不改状态→8 人并发全命中, current_price 被各自陈旧读覆盖（评审实证: 实降 8.88 元 vs 帮砍流水和 39.96 元, cut_count 已满而 status 仍"砍价中"）→ Cut 收进**事务 + LockUpdate 当前读**重算（承接 013 I-1 结论: 事务内普通读被读视图钉住）; 交付测试用同一 u2 只走了唯一键路径→改**不同 uid** 并断言 Σ(cut_amount)==起始价−当前价。**I1** assist per_limit>1 因 uk_activity_user 结构性不可达且第 2 次发起报 10002 → 1062→50006 + 注释对齐 schema（每人一次; 批次 10 后台配置须按此约束）。**I2** 秒杀 per_limit 下单侧零强制（评审: 同会员买 4 件占限量 40%）→ 下单事务内按订单项累计（status≠90）校验。**I3** 满减列表三处不符契约 → ScopeDesc/EndTime 补 api 出口、SpuId 范围过滤落地（全场/分类/商品三级判定）、分页接通（原控制器硬编码 PageSize=100）。**I4** 砍价成交价无人消费（spec 006 FR-006 跨批缺口, 账上漏记）→ 就地接通: status=2 校验 + current_price 计价 + qty=1 + 成交后条件置位 3/回填 order_no（`CodeBargainUnpayable=40004` 本为此预留）; Cut 到底价补写 success_time（M1: Launch 首刀到底同判）。**I5** 售后退货回补商品库存不回补活动库存→双账分歧 → 完成副作用按**订单项** flash_sale_item_id 回补 sold_count。**I6 记账更正+返工**: 迁移 000040 本可避免——`trade_order_item.flash_sale_item_id` 自 000023 已存在且带 idx_flash_item（本表上行"必要性"论证只驳倒了"按 sku 反查", 未查既有列; 本人 SQL 复核属实）→ **迁移 000042** 撤销订单头死列, 下单改写订单项既有列, cancelBy/售后回补改读行级归属。**Minor**: M2 流水与推进同事务（砍价+助力）; M3 取消回补判行数; M4 清理测试残留 TF-秒杀已结束b+补 defer; M5 防篡改测试改真断言（原 AssertNil 放过）; M6 重复 defer; M7 秒杀 Upcoming 补 api 出口; M9 scopeDescOf 错误传播 + Index 分块失败告警; M10 spuBriefs 复用 firstImage 口径; M11 白名单 X-Url-Path 防线注释; M12 可达性"不带凭证"对照组覆盖全部 4 会员端点; M13 账目勘误（测试文件 3 个非 4; shop 42 端点非 41; quickstart/data-model/plan 迁移陈述同步）。**M8 记账待办**: 列表内档位/范围/SPU 简介仍为每活动一次查询（N+1, 一页约 +3N）——量级可接受, 留批次 10/13 收。**契约微扩（记账）**: `FullReductionListReq` +PageReq、`FullReductionItem` +scopeDesc/endTime、`FlashSaleItem` +upcoming、`IMarketingLogic.PublicFullReductions` +spuId 参数 | 独立评审 agent 以真实库探针 + 临时 worktree 变异实验给出（C1/C2/I1~I4 均有可复现输出）; 按用户既有指令"需要裁定的按你推荐的执行"处置 | |
| 2026-09-22 | 10 | **独立评审（第五批次）: No → 修复轮, 逐条处置**。**C1 validType=2 券使 AdminList/AdminDetail nil 解引用 panic**（`*r[ValidStartAt].GTime()` 对 NULL 解引用; 评审探针实证: 建"领取后 N 天"券——spec US1 场景 3 的一等功能——列表即 500; 交付测试建了 type-2 券但从不列表故全绿）→ GTime nil 守卫（NULL 日期不参与 ValidDesc 组装）+ 补"validType=2 券可列表"用例。**C2 秒杀已售保护被自家实现击穿**（LockUpdate 只拦"移除", 随后**全删重插**把保留的已售行也删了重建 → sold_count 清零、`trade_order_item.flash_sale_item_id` 悬空 → 限购累计/取消/售后回补全部失配; 评审探针: 行 id 321→322、sold_count 1→0; 恰是 plan D2 声称要防的账实悬空; 交付测试只测"移除被拒"未测"保留替换"）→ SetItems 改 **diff 语义**: 保留行**原位 UPDATE**（价格/限量/限购, sold_count 天然不动）、删除提交集合外的行（已售保护约束不变）、新 SKU INSERT; 补"保留已售行替换 → 行 id 不变 + sold_count 守恒"断言。**I3 同值 UPDATE 误报"不存在"只修了满减一处**（FlashSale/GroupBuy/Bargain/Assist 四处 Update 仍以 affected=0 判 50003, 与完成记账声明不符——账实不符点）→ 四处统一前置存在性 Count + 幂等成功。**I4 端点级测试结构性守不住权限挂载**（凭证是超管=is_super 直通, 漏挂 RequirePerm 永不产生 10005; 且 doReq 只实现 GET/POST——13 个 PUT/DELETE 探活实发 GET 404 空洞通过; 评审变异: 删掉 RequirePerm 测试仍绿; 原"挂接守卫"声明不成立）→ ①doReq 补 PUT/DELETE 真实方法; ②增**非超管对照**（is_super=0 无角色 → 写端点必须 10005——漏挂/错挂权限即红）; ③声明勘误。**I5 满 X 减 Y 无 Y≤X 校验**（"满100减200"可配置; 计价无封顶属 012 文件——组合成负应付配置路径, 配置面校验属本批）→ fullReductionCheck 补 discount≤threshold + 用例; 计价封顶挂账。**I6 券/活动 Update "未传 status 视为停发"危险**（部分更新改名不传 status → 静默停发; 注释以"JSON 无三态"辩解不成立——*int 即三态）→ **D3-⑨ 契约微扩**: 6 个 Update Req 的 Status 改 `*int`（nil=不改启停）, DTO 同步, controller 直透。**Minor**: M7 PublicList 死契约→补实现（模板级分页, 首页券同口径; "canReceive 会员态补充"属消费端后续）+ 接口满足性编译断言（`var _ ICouponLogic = ...`）; M8 时间窗部分更新不可达且报错误导→check 改非空侧校验/双非空才比较; M10 测试残留 TF-占位秒杀补 defer; M11 死代码随 *int 重构消除; M13 拼团/砍价创建补 SpuId 存在性校验+助力缺失券名兜底文案。**记账待办**: M9（列表无法只筛停发态——契约 Status 0=全部/1=启用的语义限制, 留批次 13 收口）; M12（FullReductionUpdate 存在性检查在事务外的管理面小竞态）; M14（并发全量替换 1213 死锁裸抛——管理面低频, 留观测/批次 13） | 独立评审以真实库探针 P1~P5 + 三组变异实验给出（两个 Critical 均为"正常使用即触发"级）; 修复轮按"需要裁定的按你推荐的执行"处置 | |
| 2026-09-22 | 10 | **复审（修复轮）: Yes, C1/C2/I3~I6 全部闭环**——每项均有复审员独立探针证据（C1 含脏行 validType=1 日期 NULL 变体; C2 探针含"保留行 id/sold_count 守恒+引用不悬空+移除仍拒+重复 SKU 拒"五连; I3 四处同值更新探针; I4 服务层+HTTP 层 gf 绑定实证 absent→nil/0→&0）; 两次变异实验证明交付测试具备 RED 能力（C2: 改回全删重插 → `EXPECT 427 == 426` 行 id 变化被抓; I4: 忽略 RequirePerm → 非超管断言行红）。全量两连跑绿、lint 0、桩数一致、探针自清。**新发现 4 Minor（不阻塞）, 处置**: ①新回归测试把券改名后清理仍按原名 → 共享库泄漏可领券（复审实测 5 张已代清）→ 就地修（清理双名）; ②diff 语义下提交 stock<sold 撞 chk_sold_le_stock 报通用码 52 非业务码（事务回滚无损, 报错不友好）→ 记账; ③本轮 3 文件 gofmt 不整洁 → 顺手 gofmt; ④两处陈旧注释与 I6 三态契约矛盾 → 就地更正。观察项挂账: 满100减100 免单档可配置（策略裁定）/PublicList 与首页券块描述格式漂移（先前既有）/routes/log 被 git 追踪（先前遗留） | 复审以独立探针 + 变异实验给出; 一行级 Minor 就地收 | |
| 2026-09-22 | **11** | **批次 11 开工（§一.1 取最小编号 ⬜ 批）+ 开工勘察记账**: 本批 23 端点 = user 渠道 11（`/distribution/*` apply/status/relations/commission-rules/records/account/account-logs/withdraws POST+GET/invite-records + `/share-code`）+ admin 渠道 12（`/admin/distributors` 列表/审核/冻结 + `/admin/commission-rules` CRUD + `/admin/commission-records` + `/admin/withdraws` 列表/审核/打款 + `/admin/invite-records`）。**契约现状**: `IDistributionLogic`（user 侧 13 方法）接口已有含详注（表清单/红线/归因口径全在文件头）; **admin 侧方法全缺**（契约缺口, 需微扩 ~10 个管理方法, 批次 01/10 D3 同例）; DTO 齐。**表 9 张全既有且列注释即实现规格**（000016/000028）: user_relation(uk_user 一人一链, **两级结构强制 ADR-0003**)/distribution_user(status 1待审 2通过 3冻结)/commission_rule(uk_scope, 商品>分类命中)/commission_record(待结算→已结算→失效/冲销, 负额冲销关联列)/user_account(**可负余额**)/account_log(只追加双快照)/withdraw_order(状态机 10→20→30→40 + 50/60, uk_channel_order 打款幂等)/invite_record/share_record。**跨域钩子**: ①正向结算——订单确认收货→SettleOrder 的钩子**不存在**, 需在 shop 域 ports.go 新增投递端口（批次 07 ICommissionReverse 同型）; ②反向冲销——ICommissionReverse 已由批次 07 投递, **user 域消费侧实现属本批**; ③结算保护期定时任务 ConfirmSettle 的触发形态待定（契约阶段裁定）。**合规红线核查清单（宪法 #1, 实现期重点测试）**: 关系链两级封顶（结构强制）/自邀与互环拒绝/佣金基数=订单项实付（不含运费/积分抵扣）/提现状态机不可逆迁移全条件更新。零迁移预期; 权限点勘察建契约时做。`specs/017-distribution-fund/` 不存在 → 先补 Spec Kit 契约 | §一.1 主线推进; 资金域敏感度最高, 红线清单先行固化 | |
| 2026-09-22 | 11 | **独立评审（第六批次）: No → 修复轮**——6 Critical + 6 Important + 8 Minor（资金域最高强度评审, 全部有探针/变异证据）。**C1 提交树不可编译**: api Status *int 修复遗漏未提交（1ef5688 快照 checkout 即编译失败, 评审员 worktree 实证）→ 本轮提交补齐（历史提交不改写, 如实记账）。**C2 分享归因三连断**: common shares 桩未接（ShareReport 零调用方）/ 列名三处错（share_record 实际列 sharer_user_id/share_channel/scene_value, 实现写 user_id/channel/scene; attribute 查不存在的 buyer_user_id 且吞错静默降级）→ 全部对齐真实 schema + 接线 common 端点; **归因语义更正（记账）**: share_record 是"分享者记录"无浏览者列, V28 原设计即"下单时归因"（trade_order.attributed_user_id/attribution_type 000028）→ attribute 改读订单归因列（就绪待下单带码接线）, 分享点击→下单归因的入口接线与 I5 同项挂账。**C3 计提并发双计提**（check-then-insert 无事务无唯一键, 评审并发探针 rows=2）→ **迁移 000043**: commission_record 补 uk_item_bene_level_rev(order_item_id,beneficiary_user_id,level,reversal_of_id)（reversal_of_id NULL 不参与唯一=原始行唯一、冲销行按回指唯一, 一键两用）+ uk_reversal_of(reversal_of_id); INSERT 1062→幂等跳过; **零迁移承诺按协议破除记账**。**C4 已结算冲销并发双扣**（评审探针: 余额 100→80 应 90）→ uk_reversal_of 唯一兜底 + 回指条件更新判行数。**C5 未结算并发冲销输家扣款**（从未入账却被扣）→ 条件置失效 n==0 时 continue（不再 fall-through 进负额路径）。**C6 打款核销冻结不判行数**（评审探针: frozen 1.00 打款 10.00 → err=nil 状态 40 冻结未扣还写失真流水）→ 核销判行数, 不足报错整事务回滚。**I1 互环绑定被接受** → BindRelation 补环检测（inviter 的上级==user → 拒绝; 链仅两级一层即闭环）+ 用例。**I2 小数比例静默截断**（2.50% 入账 2.00）→ rateOf 改回传字符串比例, 计提按比例×100 整数域 /10000 精算, record.rate 存原值。**I3 冲销整单全额忽略 refundFen** → 适配器改按 after_sale_order.order_item_id 精确冲销（与 spec"该项退款→该项冲销"对齐, 端口契约 refundFen 注释同步更正为参考量）。**I5 入口链未接线**（注册不带邀请码/invite_record 无写入方/BindRelation 生产零调用）→ **挂账**: 分享带码→注册绑定→首单激励的入口接线属跨批（批次 12/13 裁定）, 本批交付服务能力（BindRelation/ShareReport/SettleOrder 就绪）。**I6 SC-4 装配测试不实** → 补 bootstrap 装配断言测试 + shop Confirm 投递 spy 测试。**Minor**: M1 结算计数排除并发输家/M2 冲销扣回判行数/M4 规则创建补 scopeType/scopeId 校验/M5 陈旧注释/M6 契约文件名勘误——就地修; **M3（审核拒绝=软删死胡同无再申请路径）/M7（昵称 N+1）/M8（LIKE 通配符未转义）记账批次 12/13**。另: **账实勘误**——tasks.md 曾勾"归因优先级测试"而交付测试无分享归因用例（本轮补 attributed 优先级用例）; PROGRESS 批次 11 完成行的"两连跑全绿"只在含未提交改动的工作区成立（C1）——均已如实更正 | 独立评审以 P1~P8 探针 + 变异实验给出; 资金并发缺陷三处（C3/C4/C5）均为真实资金风险, 修复轮以唯一索引为正解 | |
| 2026-09-22 | 11 | **复审（修复轮）: No（有条件）→ 二次收口**——6 Critical 全部闭环（独立并发探针: 双计提恰1行/双扣堵死/输家不扣/冻结不符拒付; 生成列设计经"先冲销后重放计提"探针审查无洞; C1 反向实证+装配测试两次变异皆红）。**新发现 4 Important 全部就地修**: ①**N1 互环检测不完备**——一层检查只堵二环, 3 阶环可成（评审探针实证: B←A、C←B 后 A 绑 C 被接受）; 注释"链仅两级查一层即闭环"为假——**user_relation 是无界深度父指针结构, 只有"计佣"封顶两级, "关系图"不封顶**; 修法: inviter 链上溯判环（步数上限防历史脏数据死循环）。②**N2 资金守卫测试失真**——C5 交付测试的 fixture 固定 spu_id=1（该 SPU 在应用库不存在）→ SettleOrder 0 行佣金 → 断言"余额不动"无论修复与否都绿（空转实锤 ZZP5）; 修法: 换 newDistFixture 真实夹具 + 断言失效行为; I3 适配器零交付测试 → 补（两项订单只冲指向项+不存在售后单兜底全单）。③**N3 迁移 000043 down 不可逆**——down 删不存在的首版键名且漏删 is_original 列（实测 ERROR 1091）→ 改实测可逆形态。④**N4 契约账实不符**——spec/plan/quickstart/tasks 仍写"零迁移"（库已 43）、PROGRESS 行把废弃的首版键名记为已落地 → 全部同步勘误。**Minor 就地**: N5 冲销输家竞态错误改幂等跳过日志（不再"需人工核对"噪音）/ N7 api 注释"游客可报"更正（行为=需登录, 已记账）/ N8 并发测试 goroutine 笔误; **N6 记账**: 生成列 is_original 未进生成物（代码走 g.Map 不受影响, 下次 gf gen dao 将出 diff, 留待下次重生成一并）。**账目勘误**: 复审实测 HEAD 桩数 admin 16/common 0/user 0/shop 0 = 16 与声明一致（common 接线后恰巧归真） | 复审以 11 项探针 + 2 次变异 + down 实测给出; 4 项 Important 均低成本就地修 | |
| 2026-09-22 | 11 | **复审收口（二次收口后 Yes 条件全部满足）**: ①N3 迁移 000043 down 改实测可逆形态（down/up 往返实证 43→42→43, is_original 生成列随 down 正确消失）; ②N1 互环改 **inviter 链上溯判环**（步数上限 64 兜底历史脏数据）+ 三阶环回归用例（B←A、C←B 后 A 绑 C 被拒）; ③N2 C5 守卫换真实夹具（原 spu_id=1 空转实锤修正）+ 断言"恰一条置失效"; I3 适配器补交付测试（两项订单只冲指向项 + 不存在售后单兜底全单 + 余额只扣 10.00）; ④N4 契约文档四处迁移陈述同步（spec SC-4/plan/quickstart V43/tasks 适配器描述）; **N5** 冲销输家竞态错误降幂等跳过日志; **N7** api 注释更正（分享上报需登录）; **N8** 并发测试 goroutine 笔误修正; **N6 记账**: is_original 生成列未进生成物（下次 gf gen dao 一并）。**账目勘误（复审 N4）**: 前行 C3 修复描述引用了**已废弃的首版键名** uk_item_bene_level_rev——实际落地为 is_original 生成列 + uk_item_bene_level_orig（"NULL 两用键"首版设计被并发测试当场证伪: MySQL 唯一索引含 NULL 行不参与唯一性检查, cnt=3）, 以本行为准 | 复审 6 Critical 闭环 + 4 Important 全部就地收口; 全量两连跑全绿、lint 0、桩数 16/0/0/0 与声明一致 | |
| 2026-09-22 | **12** | **批次 12 开工（§一.1 取最小编号 ⬜ 批）+ 开工勘察记账**: 本批 13 端点全在 admin 渠道——会员管理 4（`/admin/members` 列表/详情/禁用启用/改绑手机号）+ 风控 7（`/admin/risk-rules` CRUD+详情 + `/admin/risk-records` 列表/申诉处理）+ 审计日志 2（`/admin/login-logs`、`/admin/operation-logs`）；剩余 3 桩为批次 13 dashboard。**接口现状**: 审计侧 `IAuditLogic`（OperationLogs/LoginLogs/Write）已有待实现；**member 管理接口（IMemberAdminLogic 4 方法）与风控管理接口（IRiskAdminLogic 7 方法）均缺** → 新建+记账（批次 10/11 D3 同例）。**表**: risk_rule/risk_record（000021 既有, 含 rule_type 1黑名单 2高频下单 3异常领券 4佣金套利 5休眠分级、action 1拦截 2标记、appeal_status 申诉列）；member 表=既有 user/distribution 等。**跨域语义**: 风控规则引擎的实现（命中判定消费端）属批次 12 的规格范围（000029 留的 IRiskHit 端口实现）——本批交付管理面 CRUD+记录/申诉；**命中引擎接线**（下单/领券/玩法入口实时判定）规格裁定: 端口消费点已在批次 09/11 预留（RiskHit 未装配降级放行）, 本批补装配+规则评估器。权限点勘察建契约时做。`specs/018-member-audit-risk/` 不存在 → 补 Spec Kit 契约 | §一.1 主线推进 | |
| 2026-09-22 | 12 | **独立评审（第七批次）: No → 修复轮**——3 Critical + 3 Important + 8 Minor（全部有探针/变异证据）。**C1 禁用写 0 登录查 2——"禁用即拒绝登录"完全不成立**: user.status 真实枚举是 `1正常 2禁用`（000001 列注释+活库实证+auth.go:157 消费点 `==2` 拒绝），我的"勘察口径 0=禁用"未查列注释属事实性错误 → 禁用后 SMS 登录照常发 token, 治理语义为空; 修复: Disable 写 2/1, memberStatusOf 直通, 列表筛选直通, 补"禁用后登录拒绝"用例。**C2 黑名单子串误拦无辜用户**: condition_expr 用 strings.Contains 匹配 `user:<id>`, 前缀 ID 全被误拦（评审探针: 规则 user:914000123 拦 9140001/91400012）→ 改逗号分隔精确解析。**C3 生产入口永远不可能被拦**: 两生产消费点传 ruleType=2（玩法）而评估器只实现 type1 黑名单 → type2 恒不命中 → "降级放行点闭合为真实判定"是名义闭合（装配前后等效放行, 只少告警）→ 评估器改**任意入口先并查 type1 黑名单**（结构正解）。**I1 评估器测试靠跨测试污染才绿且套件泄漏启用规则**（riskCleanupRule"删后插"语义使 defer 留污染; TestRiskHitEvaluator 首场景自身 fixture 无目标规则, 清库即红——批次 11 N2"绿错了原因"同型）→ fixture 重构: 纯 DELETE 清理 + 独立 seed + 自足夹具。**I2 申诉中(1)记录死锁**（守卫只放行 0, 申诉中永无法 adjudicate, 错误文案失实）→ 守卫改 WhereIn(0,1)。**I3 SC-3"申诉通过→放行"未实现未测**（通过后规则仍在→重新判中再落事件继续拦; schema 注释"2申诉通过(解除拦截)"与实现相反）→ 落地: Hit 命中前查该用户该规则是否存在 appeal_status=2 通过记录 → 跳过拦截（白名单语义）; 补全链测试钉住。**Minor**: M1 status 值域校验/M2 申诉 remark 必填/M5 详情补 deleted=0/M8 max-length——就地修; M3 事件列表 N+1/M4 keyword 只实现昵称+`_` 未转义/M6 评估器 object_type 语义失真——记账; **M7 记账违约（§五缺完成行）**→ 本行即补 + 台账流程自我修正。**教训固化**: "勘察口径"必须落到列注释证据（C1）; 端口闭合="生产消费点行为变化"而非"装配存在"（C3） | 独立评审以探针 P1~P8 + 两次变异给出; 三 Critical 均为"批次核心语义未成立"级 | |
| 2026-09-22 | 12 | **复审收口（4 项 Important 就地收口, 3 Critical + I1~I3 全闭环确认）**: C1 禁用写 2（登录侧 status==2 拒绝闭环）+ 直通映射; C2 黑名单精确解析（逗号分隔 user:<id> 列表, 前缀 ID 不误拦）; C3 评估器**任意入口并查 type1 黑名单**（生产入口 ruleType=2 也吃黑名单——名义闭合变真实判定）; I1 fixture 重构（纯 DELETE + 独立 seed, 套件不再泄漏启用规则）; I2 申诉守卫 WhereIn(0,1)（申诉中可 adjudicate）; I3 申诉通过放行落地（Hit 命中前查 appeal_status=2 → 跳过, 对齐 schema 注释与 SC-3）+ 测试钉住; M1 状态值域/M2 申诉 remark 兜底/M5 详情补 deleted/M8 顺手。**共享库偶发**: TestReviewEdgeCoverage 一次失败次轮自愈（并行窗口, 与本批改动无关的已知偶发形态, 连续四轮全量三绿） | 修复轮+收口全量四跑全绿（含两连跑×2）、lint 0、桩数 16→3 稳定 | |
| 2026-09-22 | **13** | **批次 13 开工（最后一批, §一.1）+ 开工勘察记账**: 本批 3 端点全在 admin——`/admin/dashboard/trade`（订单数/销售额/退款额/待发货）、`/dashboard/member`（新增/活跃/休眠≥90天）、`/dashboard/product`（在售/低库存/待审核评价）。**接口已有**（`IDashboardLogic` Trade/Member/Product, system/ops.go）; **DTO 与 api 逐字段同形**（TradeDashboard/MemberDashboard/ProductDashboard）; 权限点 `dashboard:read` 已就位（000032+consts）。**零迁移零权限种子**。口径来源（全部走既有权威查询, 不自造 SQL 口径）: 销售额=trade_order pay_amount 已支付口径/待发货=status 20/退款额=after_sale_order 实退合计（status=50）; 新增=user.created_at 窗口/休眠=last_active_at ≥90天（与 000029 休眠分级口径同源）; 在售=product_spu status=1/低库存=inventory 预警条件（复用 inventory_impl 的 available<=warn_count 口径）/待审核=product_review audit_status=0。同时本批承担**全量收口终验**: 210 桩清零对账 + 12 批全量回归 + 批次 12 修复轮的复审确认并入 | §一.1 主线推进; 权限 dashboard:read 已就位 | |
| 2026-09-22 | 13 | **批次 13 实施完成（3 端点清零 → 210 桩全部清零, 13 批收官）**: `dashboard_impl.go` 三看板（Trade/Member/Product）; 口径全部锚定既有域权威查询（**已支付枚举 20/30/40 计销售额**——收口终验 C1 修正, 原 `>=20` 误含已取消 90/售后完成 status=50 计退款额/休眠 last_active_at≥**配置阈值**（`dormant.tier1.days` 缺省 90, 与 user/wx.go 同源; 休眠定义在 **000027**）/低库存复用 inventory available<=warn_count/待审核评价 audit_status=0）; 连线 3 桩。**收口终验（SC-4 全项）**: ①`check-stub` 四渠道 **0/0/0/0**（210 桩清零）; ②controller 文件 218 = api 端点定义 218（admin 129/common 8/shop 42/user 39）= §二 基线快照; ③迁移账本 schema_migrations=43 = migrations .up.sql 43 个（dirty=0）; ④全量两连跑全绿 + `golangci-lint` 0 issues; ⑤13 批全部 ✅。**实现期发现**: `Sum()` 返回 float64 违反"禁 float 存算金额"铁律 → 改 `Value("COALESCE(SUM(...),0)")` + money 规范化到分。**端点级测试**: 3 看板超管可达 + 非超管 10005 对照 | 最后一批; 13 批 210 桩全清 | |
| 2026-09-22 | 13 | **独立评审（第八批次/收口终验）: No → 修复轮**——2 Critical + 6 Important。**C1 交易看板把已取消单计入销售额**: `status >= 20` 把 **90=已取消**（000006 列注释; cancelBy 只改状态**不清零 pay_amount**）计入 orderCount/salesAmount——用户取消/后台取消/30 分钟超时自动取消的单的"应付额"被计成销售额, 运营数字系统性虚增（评审探针: status=90 pay_amount=888 → 销售额 +888）→ 改 `status IN (20,30,40)`。**C2 迁移 000041 双向不可逆**（up 在存在 `order_no=''` 行时报 1048 且留 dirty——**该迁移自述目的"既有 '' 规整为 NULL"在任何非空环境从未真正执行**; down 在 2 行 NULL 时报 1062）→ up 改**先 MODIFY 列可空再 UPDATE 置 NULL**（顺序修正）; down 改先合并 NULL 为空串再改回 NOT NULL（或标注不可回滚的分歧处理）; 补双向往返实测。**I1 微信登录不校验 status=2**——批次 12 C1"禁用后无法登录"在小程序主通道（微信静默登录 wx.go 三处 Create）不成立（评审探针: status=2 用户 WxLogin 仍发 Token）→ 三处发凭证前补 status 校验（与 SMS 同口径）。**I2 看板测试不抗并行包**（全库聚合差值 + 共享库并行 → 三次全量跑 1 红; SC-2"两连跑全绿"不可复现）→ 改用受控夹具/去全库聚合依赖。**I3 时间窗路径零测试**（全部用例只传空窗口; PendingReview 无断言; 两处空转断言 `>=1`/`>=base`——正是 C1 漏网之因）→ 补窗口用例与非空断言。**I4 activeCount 空窗口语义三处不一致**（spec"全量" vs api"周期内" vs 实现"近30天"）→ 统一为"近 30 天"并同步 spec/api dc。**I5 休眠阈值硬编码 90 未读 `dormant.tier1.days`**（权威实现在 user/wx.go）+ **引用迁移号错误**（000029 是 bargain_assist, 休眠定义在 000027）→ 读配置 + 勘误。**I6 "留批次 13 收口"的遗留项无人接手**（批次 09 满减 scope 计价/批次 10 M9/M12/M14 等指针悬空）→ **收官前重新指派为"跨批遗留登记（无主债务清单）"**并明确归属。**Minor**: M1 批次 13 完成 commit 未回填/M2 §五 表格被空行截断（均本行修复）/M3 非法窗口静默全零 + 可达断言不排 500/M4 退款额按 created_at 开窗 + mustFen 静默归 0/M5 工作区残留 .fix_b12b.py——就地清理。**批次 12 C1/C2/C3 复核**: C2/C3 已闭环; C1 **半闭合**（写 2/1+列表筛选+SMS 拒绝 ✅, 微信通道缺——I1 即此）。**可取之处保留**: 端点对账 218↔218 名字级零差/Value+COALESCE 转义实证正确/金额全链路无 float/SQL 注入实证安全/权限挂载变异有 RED/000042/000043 带数据往返可逆 | 评审以探针 P1~P8 + 双向迁移实测 + 变异给出; C1/C2 均"正常使用即触发"级 | |
| 2026-09-22 | 13 | **收口终验修复轮落地（2 Critical + 6 Important 全闭环）+ 13 批正式收官**: **C1** 看板改 `status IN (20,30,40)`（补"90 已取消单不计入"断言——原测试只造 20/40/10 故漏网）; **C2** 迁移 000041 双向修正 + **实测往返**（down 到 40 → up 到 43, dirty=0; up 带 `order_no=''` 行成功转 NULL——原报 1048 的场景已消除）; **I1** 微信登录两处发凭证分支补 `status==2` 拒绝（第三处为新用户注册, status=1 by construction）; **I2** 看板测试改**受控时间窗夹具**（去全库聚合依赖——原与 shop 并行包竞争致 1/3 全量跑红）; **I3** 补时间窗路径用例 + PendingReview 断言 + 消除两处空转断言; **I4** activeCount 口径统一"近 30 天"并同步 spec/api; **I5** 休眠阈值读配置 + 迁移号勘误（000027）; **I6** 新增 **§六 跨批遗留登记**（21 项 P0/P1/P2 分级归属, 消除"留批次 13"悬空指针）; **M1/M2** 完成 commit 回填 + 表格结构修复; **M5** 残留脚本清理。验证: 全量两连跑全绿 + lint 0 + 迁移往返 dirty=0 | 13 批 210 桩清零正式收官; 遗留债务见 §六 | |
| 2026-09-22 | 13 | **极小收口轮落地（复审 8 项全闭环）+ 13 批正式可收官**: **N1/I5** 休眠阈值改 `cfgIntSystem`（**SQL 查 system_config**, 与 user/wx.go 的 cfgInt 同源——原 `g.Cfg()` 读法该键只存在于表内 → 恒取兜底 90, 与原硬编码等价且运营改阈值后门禁/看板分叉）+ **变验证实守卫能红**; **N2/I2** 看板测试改**基线差量**（原受控窗口含 NOW → 并行包写入即窗内, 复审受控并发实验 3/3 红）+ 三连跑全绿; **N3 同族治理缺口就地收口**——**禁用不终止既有凭证**（原**只验 admin 渠道**; 会员禁用后既有 access token 仍可调用、refresh 可无限续期绕过）→ ①user 域 `ActiveUser`（与 system.ActiveAdmin 同型）②中间件 user 渠道每请求加验 ③refresh 前置校验 + `SessionManager.PeekUserId`（非消费式解析）④守卫测试 `TestDisabledMemberSessionRejected`; **I4** api dc 同步 + 补 activeCount 断言（变异验证已）; **M2/N5** §五 表内空行删除 + 修复轮行移回 §五; **N7** 000041 down 注释按**实际行为**改写（明示 `LEGACY-` 占位=有损回滚）; **N8** 019 tasks 三处勘误（20/30/40、000027、schema_migrations 驱动语义）; **N4/I6** 清 016 两处悬空指针 + §六 补登 5 项漏登债务（拼团成团/助力发奖/改密吊销/门店自提/迁移有损回滚）+ N3 项标 ✅。验证: 全量**三连跑全绿** + lint 0 + 桩 0 + 迁移往返 dirty=0 | 复审 8 项全部低成本收口; 13 批收官 | |
| 2026-09-22 | 13 | **第三轮收口（复审 3 项否定全部闭环）——13 批正式收官**: **N2 根因修复**（前两轮都错在"窗口含 NOW"——①精确等值被并行写入撞; ②基线差量吸收不了 base→after 之间的并发增删, 复审决定性实验 10/10 红）→ **夹具窗口改固定历史区间 2000-01-01 + 夹具行显式写 created_at=窗内值**（与并行写入 created_at=NOW **值域不相交**——真隔离靠值域不相交而非时间先后）+ 断言回到精确等值（更强）; **复现复审决定性实验**: 并发写入下 `TestDashboardTrade -count=5` 全绿（原 10/10 红）; **I4 断言空转修复**: 补"100 天前活跃**不得**计入 ActiveCount"（判别式断言——原"1 天前活跃+1"在两种实现下都 +1, 原理不可区分）+ **变异验证能红**; **N8 补足 3/3**: tasks:26 的 `status>=20` → 20/30/40、`dashboard_impl.go` 头注释、api `DormantCount` dc、spec 陈述面四处同步; **台账失真自纠**: 本行取代此前的"8 项全闭环/变异验证已"表述——经复审证明其中 N2/I4/N8 三项当时不成立, 现以可复现证据（并发写入实验 + 变异录像）为准。验证: 全量**三连跑全绿** + lint 0 + 桩 0 | 生产代码零缺陷（复审确认）; 三轮收口全在"测试可验证性 + 台账真实性" | |

## 六、跨批遗留登记（13 批收官后的债务归属）

> 13 批结束，"留批次 13/后续批次"这类指针**再无接续批次**——本节把它们收成**明确的归属与处置级别**，
> 避免成为无主债务。分级：**P0** 上线前必修（合规/资金/安全） / **P1** 上线前应修（正确性/口径） / **P2** 迭代优化。

| 级 | 来源批 | 事项 | 影响 | 归属/处置 |
|---|---|---|---|---|
| **P0** | 07 | 退款回调端点**无验签、无金额校验**（对比支付回调）而该路径触发不可逆副作用（回补/冲销/退款状态） | 伪造回调可致资损 | **接真实渠道前必修** |
| **P0** | 08 | **UGC 审核面缺失**（V1 评价自动通过；`IReviewLogic.Audit/PendingAudit/Reply` 无端点） | 内容审核合规责任缺口 | **公开上线前必修**（补后台审核面并切回"先审后发"） |
| **P0** | 11 | 分销**入口链未接线**（分享带码→注册绑定→首单激励；`invite_record` 无生产写入方、`BindRelation` 无调用方、SmsLoginReq 无 code 字段） | 关系链在真实系统恒空 → 佣金永不发生 | **上线前必修**（本批已交付服务能力，仅差入口接线） |
| **P0** | 12 | 风控 type 2~5（高频下单/异常领券/佣金套利/休眠分级）**无实时统计判定**（V1 记录面完整、判定不命中） | 被刷重灾区仅黑名单可拦 | **上线前应补**（规则引擎增强） |
| **P1** | 09 | **满减 scope 下单计价不认范围**（`calcFullReductionFen` 不看 `promotion_activity_scope`；"指定商品满减"对任意商品生效；且只取 id 最小活动非最优档） | 优惠口径错误/资损面 | 012 文件，**独立横切批**（配置面已正确） |
| **P1** | 06 | 下单**三段联动未接**（券核销/积分扣减/余额冻结只算不扣） | 券/积分/余额可重复使用 | 账户域批次（11 已交付账户能力，需接线） |
| **P1** | 06 | 回调**资金异常人工收口路径**缺失（旧单被扣款只告警无对账入口） | 资损不可追 | 运维/对账面板 |
| **P1** | 11 | 券/活动 Update 的 "status 未传" 已在批次 10 修为三态，但**同型语义在其余管理面**需复查 | 静默变更风险 | 横切复查 |
| **P1** | 11 | 结算保护期定时任务（`ConfirmSettle`）**无调度**（V1 手动/测试触发） | 佣金不会自动入账 | 部署面（main.go 定时器或外部 cron） |
| **P1** | 11 | 生成列 `is_original` **未进生成物**（dao/model） | `gf gen dao` 会出 diff | 下次重生成时一并 |
| **P1** | 11 | 提现打款**人工登记**（真实渠道商家转账未对接） | 运营人工成本 | 渠道对接批 |
| **P2** | 10/11/12 | 列表 **N+1**（满减档位/范围、昵称装配、风控规则名） | 管理面性能 | 优化批 |
| **P2** | 10 | 管理列表**无法只筛停发态**（Status 0=全部语义限制） | 运营便利 | 契约微扩 |
| **P2** | 10/13 | 管理面**并发替换 1213 死锁裸抛**、存在性检查事务外 TOCTOU | 低频报错不友好 | 优化批 |
| **P2** | 11 | 审核拒绝置软删成**死胡同**（无再申请路径，状态自相矛盾） | 运营流程 | 迭代 |
| **P2** | 11 | 砍价成交单取消后**权益不回退**（bargain_record 恒 3） | 策略未定义 | 策略裁定 |
| **P2** | 08 | 追评图片**既无存储列也无出参**（api 契约自相矛盾）；软删评价与 `uk_order_item` 语义分裂 | 功能缺口 | 契约修正 |
| **P2** | 13 | 非法时间窗**静默全零**；退款额按 `created_at` 而非 `refund_time` 开窗；`mustFen` 解析失败静默归 0 | 统计口径细节 | 优化批 |
| **P2** | 12 | 事件列表 `object_type=1` 语义失真（玩法命中填订单域常量）；`keyword` 只实现昵称且 `_` 未转义；api `max-length` 缺口 | 数据语义 | 优化批 |
| **P2** | 04 | 公开路径不解析 Bearer（`/shop/products/{id}` 的 viewerUserId 恒 0 → 会员足迹在该链路是死代码） | 功能缺口 | 中间件横切 |
| **P1** | 12/13 | **禁用不终止既有凭证**（N3 收口已落地: 中间件 user 渠道加验 `ActiveUser` + refresh 前置校验 + `PeekUserId`; 守卫测试 `TestDisabledMemberSessionRejected`） | 禁用可被绕过续期 | ✅ **已于批次 13 收口轮闭环** |
| **P1** | 16/09 | **拼团成团/解散/退款 + 参与记录管理**（`group_buy_team` 只读引用） | 玩法半闭环（下单可参团, 无成团/退款） | 独立玩法批 |
| **P1** | 15/16 | **助力发奖实际发放**（券/积分入账; `IAssistReward` 端口只投递意图） | 达标不发奖 | 账户域接线批 |
| **P2** | 01 | **改密不吊销其他会话**（`007-admin-base` 遗留） | 安全细节 | 安全增强 |
| **P2** | 08 | **门店自提交易侧裁决**（`008-store` 遗留） | 自提履约未闭环 | 交易增强 |
| **P2** | 13 | **迁移 000041 的 down 对多 NULL 行有损**（回填 `LEGACY-<id>` 占位, 语义降级） | 回滚后需人工核对 | 已在迁移注释明示 |
| **P1** | 08 | **`TestReviewEdgeCoverage` 偶发失败（约 1/6 全量跑）**——共享库并行包干扰, 单跑必过、复跑自愈; **批次 13 之前既有**（批次 11 期间已观察一次, 非本批引入）; 未捕获到确切断言（4 次复跑未再现, 未能定位）；方向: 该用例的列表/昵称断言可能依赖被并行包改动的公共行 | 验收可复现性（DoD"两连跑"偶发不成立） | **测试稳定性批**（需先捕获断言） |
| **P2** | 13 | 跨包**共享配置**隐患: `TestDashboardDormantThresholdFromConfig` 临时改 `system_config.dormant.tier1.days` 而并行 user 包读同一键（**取值方向已选安全侧: 调小**——只会让更多用户判休眠, 与并行期望同向, 不制造假失败; 严禁调大） | 潜在 flake | 已注释说明; 通用隐患: 共享配置的跨包读写 |
| **P2** | 02 | `IStoreLogic`/`ICartLogic`/`IReviewLogic` 等**无实现者的死契约**（文字承诺） | 代码整洁 | 清理批 |
