# ECBOOT 电商

社交电商平台（小程序/H5 商城 + 后台管理）。本文件是全项目的统一语言（术语表），
代码、文档、沟通一律使用这些词，避免同义词漂移。

## Language

### 用户与账户

**用户（User）**:
在 C 端商城注册、购物的人，以手机号为主凭证，可绑定微信。
_Avoid_: 会员、客户、买家、账号

**收货地址（Address）**:
用户维护的收件人信息，一条可被设为默认；下单时被复制成订单快照。
_Avoid_: 地址簿、配送信息

### 商品

**商品（Product）**:
SPU 与 SKU 的统称。日常沟通说"商品"时须能区分指哪一层。
_Avoid_: 货品、宝贝

**SPU（标准产品单元）**:
一款商品的本体：名称、分类、品牌、图文、规格定义。不直接定价、不可购买。
_Avoid_: 商品本体、父商品

**SKU**:
一个规格组合对应的最小销售单元，有价格、可上下架、有库存。用户下单买的是 SKU。
_Avoid_: 规格、变体、子商品

**规格（Spec）**:
销售维度（颜色/尺码）及其取值。SPU 声明规格轴与可选值，SKU 持有一组具体取值。
_Avoid_: 属性（属性指非销售特征，如材质）

**上下架**:
商品的可售开关，分两级：SPU 整体、SKU 单品。可售 = SPU 上架 且 SKU 启用 且 有库存。

### 库存

**库存（Inventory）**:
针对 SKU 的数量账：总库存、锁定数；可售 = 总库存 − 锁定。不以"库存"指代具体数字时须带前缀。
_Avoid_: 存量、货量

**锁定（Lock）**:
下单未支付期间被占用的库存；支付则核销，取消/超时则释放。
_Avoid_: 预占、冻结

### 交易

**订单（Order）**:
一次购买行为的凭证，含收货快照与金额账。取消是订单的一种终态，不是删除。
_Avoid_: 交易、单子

**订单项（Order Item）**:
订单中的一行商品，冻结下单时刻的商品名称、规格、单价等（快照），是售后金额的依据。
_Avoid_: 子订单、商品行

**快照（Snapshot）**:
下单时对可变信息（商品、地址）的定格复制；此后源数据变化不影响订单。
_Avoid_: 备份、冗余（冗余指可重建的数据，快照不可）

**支付单（Pay Order）**:
向支付渠道发起的一次支付尝试。一笔订单可有多张支付单（失败重发起），同时至多一张待支付。
_Avoid_: 付款、流水（流水指 log 类记录）

**售后单（After-Sale Order）**:
针对某个订单项发起的退款/退货退款申请，独立于订单主状态机运转。
_Avoid_: 退款单（仅指渠道退款动作）、工单

### 促销

**优惠券（Coupon）**:
运营发放的抵扣凭证模板；用户领到的实例称"用户券"。核销与订单同事务，取消则退回。
_Avoid_: 券、红包

### 运费

**运费模板（Freight Template）**:
计算运费的规则集（按件数或按重量计费，可按省份差异化），满额可包邮。下单时按模板算出运费并快照进订单。
_Avoid_: 物流模板、邮费规则

### 运营后台

**后台账号（Admin User）**:
运营/管理人员登录后台的身份，与 C 端用户完全隔离；超级管理员跳过权限校验。
_Avoid_: 管理员、员工、管理用户

**角色（Role）**:
一组权限的命名集合，后台账号通过角色获得权限；一个账号可拥有多个角色。
_Avoid_: 用户组、身份

**权限（Permission）**:
后台的一个可控制点；菜单、按钮、接口统一为一棵权限树，以编码（如 product:spu:create）标识。
_Avoid_: 资源、功能点

**审计日志（Audit Log）**:
后台登录与操作的全量留档，只追加、不可修改，用于追溯与合规。含失败尝试。
_Avoid_: 操作记录、系统日志（指技术日志）
