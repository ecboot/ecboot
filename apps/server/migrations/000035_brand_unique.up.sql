-- ============================================================
-- 000035 品牌名称唯一约束 (特性005评审发现: FR-008 建表漏了唯一索引)
-- 语义: 存活期间名称唯一, 软删后仍占用(ADR-0002 严格唯一策略)
-- 存量数据环境: 若已有重名品牌, 需先清洗再执行本迁移
-- ============================================================

ALTER TABLE `product_brand`
  ADD UNIQUE KEY `uk_name` (`name`);
